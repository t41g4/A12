# PROJECT_SPEC.md
# シェアサイクル企業向け「要メンテ車両」自動検知システム

## 0. 文書管理

```text
文書種別      ：プロジェクト基本仕様書
版数          ：第3版
ステータス    ：実験前仕様
対象フェーズ  ：研究・PoC・MVP
主要検知対象  ：重度パンク
```

### 0.1 第3版における主要な変更

第2版から以下を変更した。変更理由は各章に記載する。

```text
 1. 速度の統制方式を変更（速度計測を要求しない方式へ）              第42章
 2. 取得品質の受入基準を FIFO 取得方式に整合する指標へ改訂          第11章
 3. タイムスタンプ分解能を µs へ変更し、クロック源を明示            第10章・第44章
 4. サブ窓有効数の基準を L = 9 へ整合（第2版の内部矛盾を修正）      第14章
 5. bin別AUC・帯域選択の評価単位を走行単位へ明確化                  第21章
 6. 走行順序のランダム化を新設                                      第41章
 7. 単一車両・単一ライダー条件における外挿限界を明記                第42章
 8. 合否判定に用いる統計量（点推定／信頼区間）を明記                第45章・第51章
 9. ネスト交差検証後の最終パラメータ確定手順を新設                  第45章
10. 取り付け角度誤差における横軸漏れ込みを追記                      第12章
11. 走行中の重力ベクトル推定手順を規定                              第30章
12. BPF+RMS の検証を「一致確認」から「閾値再導出」へ変更            第27章
13. k = 2 における誤検知率の定量、および誤検知の時間相関を注記      第23章
14. k = 1 選択時の SUSPECT 定義と2走行ルールの扱いを規定            第29章
15. 2走行ルール適用時の性能を PoC 報告へ併記することを規定          第51章
16. 保留事項（新規実測データ確認後に決定する項目）を新設            第58章
```

### 0.2 本仕様書における確定・暫定・保留の区別

本仕様書の記載は以下の3区分に分類される。

```text
確定仕様：現時点で変更を予定しない。第52章に集約
暫定仕様：実証実験の結果により変更される。第53章に集約
保留事項：新たに取得された実測データの確認後に決定する。第58章に集約
```

第2版で確定仕様とされていたセンサ設定の一部（サンプリング周波数・加速度レンジ・内蔵DLPF・取得品質の受入基準値）は、その根拠が実験前実証データに依存するため、第3版では**保留事項**として第58章へ整理した。ただし記載内容そのものは第2版から変更していない。新規データの確認後にあらためて確定または改訂する。

---

# 1. 企画概要

本システムは、自転車に搭載した振動センサーとエッジコンピューティングを用いて走行中の異常振動を検知し、整備が必要と推定された車両を自動的に貸出対象から除外するシステムである。

基本フロー：

```text
利用者が走行
        ↓
MPU6050で加速度取得
        ↓
ESP32で5秒ごとに帯域振動強度を算出
        ↓
閾値判定
        ↓
走行単位で異常確定
        ↓
異常フラグ保持（ラッチ）
        ↓
車両返却・施錠
        ↓
Wi-Fi接続
        ↓
Firebaseへ送信
        ↓
要メンテ車両へ更新
        ↓
貸出・予約を制限
        ↓
保守担当者が点検
        ↓
修理・確認
        ↓
貸出可能へ復旧
```

---

# 2. 解決する課題

シェアサイクルでは、車体異常が発生しても利用者が申告せず返却する場合がある。

その結果、

- パンク車両が再貸出される
- 故障車両を利用者が選択してしまう
- CS問い合わせが増加する
- クレームが発生する
- 利用者満足度が低下する
- 事故・ケガにつながる可能性がある

という問題が発生する。

---

# 3. プロジェクト目的

## 3.1 ユーザー側

「借りた自転車が故障していた」という負のユーザー体験を減少させる。

## 3.2 運営側

故障車両の発見を自動化し、保守担当者が点検すべき車両を効率的に抽出する。

## 3.3 安全面

明らかな異常状態の車両を次のユーザーへ貸し出す可能性を低減する。

---

# 4. システム設計思想

本システムは、

**高価な高精度センサーによる100%の故障診断システム**

を目指すものではない。

低コストなセンサーを大量のシェアサイクル車両へ搭載できることを重視する。

したがって、

```text
低コストセンサー
+
エッジ判定
+
ユーザー申告
+
保守担当者による最終確認
```

を組み合わせた運用を前提とする。

また、アルゴリズムは可能な限り単純に保つ。複雑なモデルは、単純な方式で目標を達成できないことが実証された場合にのみ導入する。

---

# 5. MVPの検知対象

## 主要対象

重度パンク。

定義：

```text
タイヤ空気圧：0〜50 kPa程度
```

目安：

- 空気が大きく抜けている
- 走行に明確な異常がある
- 段差等でリムへの衝撃が発生する可能性がある

---

# 6. 状態レベル

| 状態 | 空気圧目安 | 説明 | MVP上の扱い |
|---|---:|---|---|
| 正常 | 300〜400 kPa | 規定空気圧 | 正常判定すべき |
| 軽度 | 200〜250 kPa | やや柔らかい | 許容領域（アラート不可） |
| 中度 | 100〜150 kPa | 明確な空気圧低下 | 許容領域（アラート不可） |
| 重度 | 0〜50 kPa | ほぼパンク状態 | 異常検知対象 |

軽度・中度の空気圧は暫定値とし、実証実験前に実測により確定する（第58章）。

軽度・中度は「アラートを出してはいけないクラス」であり、第51章の `FPR_field` の計算対象に含める。

---

# 7. 評価目標

## 7.1 重度パンク検知率

目標：

```text
Recall_run >= 65%
```

定義：

```text
Recall_run = TP / (TP + FN)
```

## 7.2 正常走行誤検知率

目標：

```text
FPR_run <= 25%
```

定義：

```text
FPR_run = FP / (FP + TN)
```

## 7.3 目標値の位置づけ

上記の目標値は先行研究から得られた暫定値である。第3版でもこの値を維持する。

ただし、これらの値は**走行単位（走行試験1回）で定義される**。5秒評価区間単位の誤検知率と混同してはならない。両者の関係については第23章および第25章を参照すること。

また、目標値の達成判定に用いる統計量（点推定値か信頼区間下限か）は第45章 45.7節に定める。

実証結果と保守業務コストを踏まえ、PoC完了後に目標値を再設定してよい。

---

# 8. 評価単位

評価は原則、

**走行試験1回単位**

とする。

例：

重度パンク状態で60回走行し、そのうち42回で異常判定された場合、

```text
Recall_run = 42 / 60 = 70%
```

となる。

正常60回中12回が異常判定された場合、

```text
FPR_run = 12 / 60 = 20%
```

となる。

1走行から得られる複数の5秒評価区間を、独立した評価サンプルとして扱ってはならない。

この原則は性能評価だけでなく、**帯域選択（第21章）および閾値決定（第24章）にも適用される**。第3版ではこの点を第21章において明示した。

---

# 9. ハードウェア構成

## センサー

MPU6050（3軸加速度・3軸ジャイロ）

## マイコン

ESP32

## 通信

Wi-Fi（返却時同期）／BLE（実験時制御）

## クラウド

Firebase

## 搭載位置

搭載位置候補：

- サドル下
- フレーム内部

最終搭載位置は振動特性実験によって決定する。ただし、決定後は全車両で統一し、第12章の取り付け姿勢規定に従う。

---

# 10. センサ設定

実装時に必ず設定・記録する。10.1〜10.3節は保留事項（第58章）であり、新規実測データの確認後に確定する。

## 10.1 サンプリング周波数（保留）

```text
fs = 128 Hz
fN = fs / 2 = 64 Hz
```

第1版の 100 Hz から変更した。理由：

1. 実測データにおいて 100 Hz が達成されておらず（実効 62〜71 Hz）、ナイキスト周波数が解析候補帯域を下回っていた
2. 128 Hz は 2 のべき乗と整合し、1秒 = 128サンプル、周波数分解能 1 Hz を素直に実現できる
3. 解析対象帯域（想定 10〜40 Hz）に対して十分な余裕を確保できる

**本値は第58章の保留事項である。** MPU6050 のサンプルレートは分周により決定されるため、公称値 128 Hz が実現可能であるかを新規データにより確認する。実現できない場合、サブ窓長 `N` および周波数分解能 `Δf` の記載も併せて改訂する。ただし帯域は bin 番号で定義するため（第17.1章）、`Δf` が厳密に 1 Hz でなくとも仕様の構造は成立する。

## 10.2 加速度レンジ（保留）

```text
AFS_SEL = 1 （±4 g）
```

第1版の実測データでは ±2 g で 0.5〜2.5% のサンプルが飽和していた。飽和は波形を矩形波化し、存在しない高調波を全帯域に発生させるため、必ず回避する。

実験段階で ±4 g でも飽和が発生する場合は ±8 g（AFS_SEL = 2）へ変更する。変更した場合は特徴量および閾値を再評価する。

**本値は第58章の保留事項である。** 新規データの `clip_rate` 実測値により確定する。

## 10.3 内蔵デジタルローパスフィルタ（保留）

```text
DLPF_CFG = 3 （帯域幅 44 Hz、遅延 4.9 ms）
```

MPU6050 の内部サンプリングは fs より高速であるため、DLPF を設定しないと帯域外成分が解析帯域へ折り返す（エイリアシング）。DLPF_CFG は必ず明示的に設定し、設定値をメタデータへ記録する。

DLPF の遅延は全サンプルに一律に作用するため、周波数解析には影響しない。

**本値は第58章の保留事項である。** `fs` との関係におけるアンチエイリアシング余裕を新規データにより確認する。

## 10.4 取得方式

```text
ハードウェアタイマ割り込み + MPU6050 FIFO バーストリード
```

第1版の実装は `loop()` 内のポーリングであり、サンプリング間隔の標準偏差が 8〜32 ms、欠損が最大 23.6% に達していた。FFTおよびPSD推定は等間隔サンプリングを前提とするため、この方式は使用しない。

SDカードへの書き込みは1サンプルごとに `open`／`close` せず、バッファリングして一括書き込みとする。

## 10.5 クロック源

本節は第3版で新設した。

```text
PWR_MGMT_1 の CLKSEL = 1 （Gyro X 軸 PLL 参照）
```

MPU6050 の既定値である内部8MHz発振器は温度依存性が大きく、サンプリング周波数が数%変動する。第11章の `fs_effective` 受入基準（公称値の ±2% 以内）は、取得系の実装不具合を検出するための基準であり、クロック源の選定ミスによる系統的ずれで消費されてはならない。

CLKSEL の設定値はメタデータへ記録する。

## 10.6 単位と分解能

```text
保存単位：m/s²（浮動小数点、小数第3位まで）
```

第2版の「小数第2位まで」から変更した。理由：現行の ±4 g 設定では 1 LSB ≈ 0.0012 m/s² であり、小数第2位（0.01）では量子化幅より粗く、情報を失うためである。

生LSB値と物理量が混在することを禁止する。生LSB値を併せて保存する場合は独立したカラムとし、カラム名に単位を明記する。

CSVヘッダおよびメタデータに、単位・AFS_SEL・DLPF_CFG・CLKSEL を必ず記録する。

---

# 11. データ取得品質の受入基準

本章は第3版で全面改訂した。以下の基準を満たさないデータは解析対象としない。

## 11.1 取得方式と受入指標の対応

第10.4章により取得方式は FIFO バーストリードである。この方式では、個々のサンプルの時刻を決めるのは MPU6050 の内部クロックであり、ESP32 側で取得できるタイムスタンプは「FIFO 読み出し時刻」である。1回の読み出しで取得した複数サンプルは、すべて同一の読み出し時刻を持つ。

したがって、ポーリング方式を前提とした `dt_std` / `dt_max` は、FIFO 方式では取得系の品質を直接表さない。第2版はこの点が整合していなかった。

受入指標は取得方式ごとに定義する。

## 11.2 FIFO 方式における受入基準（本仕様の標準）

| 項目 | 記号 | 受入基準 |
|---|---|---|
| FIFOオーバーフロー回数 | `fifo_overflow_count` | 0 |
| 欠損サンプル数 | `n_dropped` | 0 |
| 実効サンプリング周波数の誤差 | `fs_effective` | 公称値の ±2% 以内 |
| FIFO読み出し間隔の最大値 | `read_interval_max_ms` | FIFO容量の 50% に相当する時間以内 |
| 飽和率（\|a\| がレンジ端に到達） | `clip_rate` | ≤ 0.01% |

`fs_effective` は、十分に長い区間（60秒以上）における総サンプル数と ESP32 タイマによる経過時間から算出する。

```text
fs_effective = n_samples_total / ( ( t_end_us - t_start_us ) / 1e6 )
```

MPU6050 の FIFO 容量は 1024 byte であり、加速度3軸 + ジャイロ3軸を格納する場合 1サンプルあたり 12 byte、最大 85 サンプルを保持できる。読み出し間隔はこの 50%（約 42 サンプル分の時間）を超えないよう設計する。オーバーフローが発生した時点でサンプルの連続性が失われ、以降の時刻再構成が成立しなくなるためである。

## 11.3 サンプル時刻の再構成

FIFO 方式では、サンプル時刻を読み出し時刻から与えてはならない。以下により再構成する。

```text
t_us[i] = t_start_us + i * 1e6 / fs_effective
```

`i` はサンプル通し番号（0始まり）である。この再構成は `fifo_overflow_count = 0` かつ `n_dropped = 0` の場合にのみ有効である。

## 11.4 ポーリング方式を用いる場合の受入基準（参考・保留）

FIFO 方式を採用できない場合に限り、以下を適用する。

| 項目 | 記号 | 受入基準 |
|---|---|---|
| サンプリング間隔の標準偏差 | `dt_std` | ≤ 1.0 ms |
| サンプリング間隔の最大値 | `dt_max` | ≤ 2 / fs |
| 欠損率（dt > 2/fs の割合） | `dropout_rate` | ≤ 0.1% |
| 飽和率 | `clip_rate` | ≤ 0.01% |
| 実効サンプリング周波数の誤差 | — | 公称値の ±2% 以内 |

これらの基準値は第58章の保留事項であり、新規データにより達成可能性を確認する。

なお、タイムスタンプを ms 分解能で記録した場合、丸めのみで `dt_std` に 0.4〜0.5 ms 程度が生じ、受入基準の半分を測定分解能で消費する。第44章のとおりタイムスタンプは µs 分解能で記録する。

## 11.5 記録と除外

これらの値は各走行のメタデータとして必ず算出・保存する。

基準を満たさない走行は、解析対象外として `excluded_reason` を記録したうえで保持する。取得系の問題を隠蔽してはならない。

**Priority 1（第55章）は、この受入基準を満たすデータを取得できる状態にすることである。これが達成されるまで、閾値決定を含む一切の性能評価を行わない。**

---

# 12. 取り付け姿勢

## 12.1 取り付け角度の規定

```text
センサーZ軸と鉛直方向のなす角：±15° 以内
```

取り付け治具によりこれを担保する。

角度誤差は2つの経路で判定へ影響する。第2版は減衰のみを記載していたが、第3版では横軸成分の漏れ込みを併記する。

```text
経路1：鉛直成分の減衰        cos θ
経路2：横方向振動の漏れ込み   sin θ
```

| 角度誤差 | 鉛直成分の振幅比 (cos θ) | 横成分の漏れ込み率 (sin θ) | 評価 |
|---:|---:|---:|---|
| ±5° | 99.6% | 8.7% | 無視できる |
| ±10° | 98.5% | 17.4% | 許容 |
| ±15° | 96.6% | 25.9% | 許容上限 |
| 30° | 86.6% | 50.0% | 不可 |
| 45° | 70.7% | 70.7% | 不可 |

減衰のみを見れば ±15° の損失は 3.4%（0.3 dB）にすぎない。しかし同じ角度で横方向振動の 25.9% が az へ漏れ込む。横方向振動が鉛直方向と同程度の大きさを持つ場合、漏れ込みによる影響のほうが減衰より大きい。

したがって、**±15° を許容上限とする根拠は減衰量ではなく、漏れ込み量が判定へ与える影響が実験で許容範囲と確認されることにある**。第13.3章の az と重力方向射影 `a_v` の比較において、この影響を評価対象に含める。

## 12.2 重力ベクトルの記録

各走行の開始時（静止状態）において、3軸の平均値から重力ベクトルを算出し、必ず記録する。

```text
g_vec = (gx, gy, gz)     静止時1秒間の3軸平均
u     = g_vec / |g_vec|  単位ベクトル
theta = arccos(u_z)      Z軸と鉛直方向のなす角
```

記録する目的は3つある。

1. 取り付け角度が規定内にあることの検証
2. 経時的なブラケットの緩みの検出（第30章）
3. 誤検知分析・見逃し分析における原因特定（第46章・第47章）

## 12.3 取り付けずれによるサイレント故障

取り付け角度がずれた場合、振幅が低下して重度パンクを検知しなくなる。しかし本システムはラッチ方式であり「検知しなかったこと」は記録に残らないため、健全な車両と区別できない。

したがって、**重力ベクトルの記録と監視は、この故障モードを検出する唯一の手段である**。取り付け角度を固定する方針であっても省略してはならない。

## 12.4 取り付け姿勢の記録項目

各走行のメタデータへ以下を保存する。

```text
gravity_x
gravity_y
gravity_z
tilt_angle_deg
mount_reference_id    （基準となる取り付け時の記録ID）
```

---

# 13. 解析信号

## 13.1 主解析信号

```text
主解析信号：az（鉛直方向加速度）
```

第12章により取り付け姿勢を固定するため、az をそのまま主解析信号とする。

## 13.2 生データの保存

```text
ax, ay, az の3軸すべてを常に保存する。
```

保存する目的：

- 主解析軸の妥当性を実験で再評価するため
- 将来の転倒・盗難検知（第55章 Priority 13）に必要なため
- 取り付け姿勢の検証に必要なため

## 13.3 実験時の比較対象

実験フェーズでは、az に加えて重力方向射影を並行計算し、分離性能を比較する。

```text
a_v[n] = ax[n]·u_x + ay[n]·u_y + az[n]·u_z
```

比較においては、分離性能（AUC）に加えて、12.1節の横軸漏れ込みに対する頑健性を評価する。具体的には、取り付け角度が異なる条件下で `L_5s` がどの程度変動するかを両者について確認する。

両者に有意差がない場合は、より単純な az を採用する。

## 13.4 3軸ノルムを使用しない理由

```text
|a| = sqrt(ax² + ay² + az²)
```

3軸ノルムは主解析信号として使用しない。

理由：ノルムは非線形演算であり、重力に対する横方向振動の比が大きい条件下で以下の展開が成立しなくなる。

```text
|g + v| ≈ |g| + (v の重力方向成分) + |v の直交成分|² / (2|g|)
                                     ↑ 非線形歪み項
```

実測データにおいて、この歪み項は欲しい信号の 48% の大きさに達し、重力方向射影との相関は 0.455 に留まった。第3項の二乗演算は元の信号に存在しない和・差の周波数成分を生成するため、帯域選択を実験で決定する本方式とは根本的に相容れない。

姿勢不変性が必要な場合は、線形演算である重力方向射影（13.3節）を用いる。

---

# 14. 前処理

## 14.1 基本処理

```text
生加速度（m/s²）
        ↓
受入基準の確認（第11章）
        ↓
欠損・飽和サンプルの検出
        ↓
サブ窓へ分割
        ↓
サブ窓ごとの平均値除去
        ↓
窓関数適用
        ↓
FFT → PSD
```

## 14.2 平均値除去

サブ窓ごとに実施する。

```text
x'[n] = x[n] - mean(x)
```

目的：

- DC成分（重力）の影響を除去する
- サブ窓間で解析条件を統一する

## 14.3 ハイパスフィルタについて

ハイパスフィルタは採用しない。

理由：解析対象は 10 Hz 以上の帯域におけるパワーであり、サブ窓ごとの平均値除去によって DC 成分は既に除去されている。カットオフ数 Hz のハイパスフィルタが帯域パワーへ与える影響は無視できる。

第1版では「平均値除去 vs HPF の比較」を Priority 1 としていたが、上記の理由により削除した。

## 14.4 異常サンプルの扱い

飽和または欠損を含むサブ窓は、PSD 平均から除外する。

第2版の記載は「5サブ窓中3サブ窓以上」であったが、第15.2章のサブ窓数 `L = 9` と矛盾していた。第1版の名残と判断し、第3版では以下へ修正する。

```text
9サブ窓中6サブ窓以上が有効な場合  → 有効サブ窓のみで平均し、評価に使用
有効サブ窓が5以下の場合           → その5秒区間を評価対象外とする
```

`L` を変更した場合は、この基準も `L` の 2/3 を目安として再定義する。

### 14.4.1 オーバーラップに起因する伝播

サブ窓は 50% オーバーラップであるため、**1サンプルの飽和は必ず2つのサブ窓を無効化する**（区間端のサブ窓を除く）。したがって、区間内に3か所以上の独立した飽和が発生すると、有効サブ窓が基準を下回る可能性がある。

重度パンクはリムへの衝撃により飽和を起こしやすいため、この規則は「最も検知しやすい区間が評価対象外になる」方向に働く。除外された区間数と `clip_rate` の関係は、第47章の見逃し分析における必須確認項目とする。

除外したサブ窓数は `n_valid_subwindows` として記録する。

---

# 15. 判定単位と時間構造

## 15.1 判定単位

```text
判定単位：5秒
```

第1版の「0.5秒窓による瞬間判定」は廃止する。0.5秒窓は判定単位として使用しない。

## 15.2 時間構造

```text
5秒評価区間
├── サブ窓1  （1秒 = N サンプル）
├── サブ窓2  （0.5秒オーバーラップ）
├── サブ窓3
├── ...
└── サブ窓9

  ↓ 各サブ窓のPSDを平均（Welch法）

5秒区間のPSD  G(k)

  ↓ 帯域積分

帯域RMS加速度  A_5s(B)

  ↓ 対数変換

主判定特徴量  L_5s
```

```text
サブ窓長      ：1秒（N = fs × 1）
オーバーラップ：50%
サブ窓数      ：L = 9
窓関数        ：Hann窓（periodic形）
周波数分解能  ：Δf = fs / N
```

`fs = 128 Hz` の場合、`N = 128`、`Δf = 1 Hz` となる。`fs` は第58章の保留事項であるため、`N` および `Δf` は `fs` から導出される値として記述する。帯域は bin 番号で定義するため（第17.1章）、`Δf` が変化しても仕様の構造は成立する。

## 15.3 サブ窓の位置づけ

**サブ窓は Welch 法における平均化のための内部処理であり、判定単位ではない。**

サブ窓単位で閾値判定を行ってはならない。判定は 5 秒区間単位でのみ実施する。

## 15.4 サブ窓長の選定理由

推定の安定性は自由度で決まり、自由度は概ね「帯域幅 × 評価時間」に比例する。したがって 5 秒を 1 回で解析しても、サブ窓へ分割して平均しても、帯域パワーの推定精度は同等である。

サブ窓へ分割する理由は精度ではなく、以下の 2 点である。

1. 飽和・欠損を含むサブ窓のみを除外できる（14.4節）
2. 走行中の路面変化に対して定常性の仮定が保たれやすい

## 15.5 評価区間の切り方

```text
5秒評価区間は重複なし（タンブリング方式）とする。
```

スライディング方式は、1走行あたりの判定機会を約10倍に増やし、走行単位の誤検知率を著しく悪化させるため使用しない。

## 15.6 評価区間数

1走行 200 m を走行した場合、第42章の走行時間帯域（48〜60 秒）において評価区間数は概ね 9〜12 区間となる。

評価区間数は走行単位の誤検知率に直接影響するため（第23.2章）、走行時間の統制は誤検知率の安定にも寄与する。

実際の区間数は走行ごとに `n_segments` として記録する。

---

# 16. パワースペクトル密度の算出

## 16.1 窓関数

Hann窓（periodic形）を使用する。

```text
w[n] = 0.5 · ( 1 - cos( 2πn / N ) )     0 <= n < N
```

periodic形（分母 N）を使用する。symmetric形（分母 N-1）は使用しない。

## 16.2 サブ窓のFFT

```text
y_l[n] = x'_l[n] · w[n]

X_l(k) = Σ_{n=0}^{N-1} y_l[n] · exp(-j2πkn/N)
```

ここで、

```text
l ：サブ窓番号（1 <= l <= L）
n ：サブ窓内サンプル番号
k ：周波数bin番号
N ：サブ窓のサンプル数
L ：サブ窓数（= 9）
```

## 16.3 片側パワースペクトル密度

```text
G(k) = (1/L) · Σ_{l=1}^{L}  2·|X_l(k)|² / ( fs · Σ_{n} w[n]² )
```

単位は `(m/s²)² / Hz`。

DC成分（k = 0）およびナイキスト成分（k = N/2）については係数 2 を適用しない。ただし、DC成分は平均値除去により既にゼロであり、解析帯域にも含まれないため実質的に使用しない。

## 16.4 PSD正規化を採用する理由

第1版では振幅スペクトルの帯域平均を使用していたが、これは正弦波に対してのみ条件間で比較可能であり、広帯域ランダム振動に対しては成立しない。広帯域信号では 1 bin あたりの振幅が bin 幅に依存するため、窓長を変えるだけで値が変化してしまう。

PSD正規化は bin 幅で除した密度であるため、**サブ窓長・窓関数・サブ窓数・オーバーラップ率を変更しても値が変わらない**。第1版第13章が宣言していた「異なるFFT条件間で比較可能にする」という目的を実際に達成できるのはこの形である。

この性質により、第58章の保留事項である `fs` が変更された場合でも、PSD の値そのものは比較可能である。

---

# 17. 帯域RMS加速度と主判定特徴量

## 17.1 帯域RMS加速度

周波数帯域 B における実効振動加速度を以下で定義する。

```text
A_5s(B) = sqrt( Σ_{k∈B} G(k) · Δf )
```

単位は `m/s²`。物理的には「その帯域に含まれる振動の実効値」を意味する。

帯域 B は bin 番号で定義する。

```text
B = { k | k_low <= k <= k_high }
```

周波数で帯域を指定する場合も、必ず bin 境界に一致させる。帯域を bin で定義することにより、`Δf` が 1 Hz でない場合にも定義が一意に定まる。メタデータには bin 番号と対応する周波数の両方を記録する。

## 17.2 主判定特徴量

```text
L_5s = 20 · log10( A_5s(B*) / A_ref )     A_ref = 1 m/s²
```

単位は dB。`B*` は第21章で決定する解析帯域である。

`A_ref = 1 m/s²` は本仕様書内で一貫して使用する基準値であり、振動評価の慣用値（ISO における 1×10⁻⁶ m/s²）とは異なる。対外説明および論文化の際は基準値を必ず明記する。

## 17.3 対数領域を採用する理由

1. 振動振幅は概ね対数正規分布に従うため、対数変換により正規分布へ近づき、ROCの挙動および閾値が安定する
2. 倍率で作用する誤差要因が、すべて定数の加減算になる

```text
取り付け角度誤差 cos θ   →  20·log10(cos θ)   = 定数シフト
速度依存性 ∝ v²          →  40·log10(v)       = 定数シフト
ライダー体重・車種差      →  定数シフト
センサー感度の個体差      →  定数シフト
```

ただし、**本フェーズでは速度を測定しないため、速度に起因するシフト量を補正することはできない**（第42章）。上記は原理の説明であり、補正手順ではない。

3. 分離性能を扱いやすい数値で表現できる（振幅比 2倍 = 6.0 dB、10倍 = 20.0 dB）

## 17.4 A_5s も併記する

`L_5s` は判定に使用するが、物理量としての `A_5s`（m/s²）も必ず併せて記録する。物理的な妥当性の検証および対外説明に必要である。

---

# 18. 帯域比（第二特徴量）

## 18.1 定義

```text
Q_dB = L_5s(B*) - L_5s(B_ref)
```

ここで `B_ref` は参照帯域であり、`B*` と重複しない低周波帯域とする。

```text
B_ref 初期候補：2〜8 Hz（保留・第58章）
```

## 18.2 採用理由

第1版の解析において、低周波帯（2〜10 Hz）は正常と異常でほぼ同一（比 1.05）である一方、識別に寄与する帯域では比が 15 倍に達することが確認された。低周波帯はペダリングおよび路面由来の共通成分と考えられる。

帯域比は分子分母で共通の定数シフトを相殺するため、以下の影響を受けにくい。

```text
取り付け角度
ライダー体重
車種差
センサー感度の個体差
速度（条件付き。18.5節を参照）
```

## 18.3 第1版の周波数集中度との違い

第1版第21章の周波数集中度は、分母を「解析帯域全体」としていたため分子が分母に含まれ、感度が鈍っていた。

```text
第1版： C = P(B*) / P(B_all)        分子が分母に含まれる
第3版： Q = P(B*) / P(B_ref)        互いに素な2帯域の比
```

互いに素な2帯域の比とすることで、識別能力を維持したまま定数シフトを相殺できる。

## 18.4 位置づけ

`Q_dB` は第二特徴量であり、MVP第一段階では主判定に使用しない。`L_5s` 単体で目標を達成できない場合に限り、第26章の手順に従って導入を検討する。

ただし、実験時には必ず計算・保存する。

## 18.5 速度相殺の成立条件

本節は第3版で新設した。

帯域比が速度の影響を相殺できるのは、`B*` と `B_ref` が速度に対して同じ依存性を持つ場合に限られる。

低周波帯には車輪回転に由来する成分が含まれる可能性がある。車輪回転の基本周波数は速度に比例して**移動する**ため、固定帯域 `B_ref` に対するその寄与は速度とともに変化する。この場合、速度に対する相殺は成立しない。

```text
26インチ車輪、周長 約2.1 m の場合
  12 km/h → 車輪回転 約1.6 Hz
  15 km/h → 車輪回転 約2.0 Hz
```

したがって `B_ref` の確定にあたっては、以下を確認する。

```text
1. 当該帯域の AUC が 0.5 近傍であること（識別に寄与しない共通成分であること）
2. 当該帯域に十分なパワーが存在すること
3. 走行時間（＝平均速度に対応）と L_5s(B_ref) の相関が小さいこと
```

`B_ref` の値そのものは第58章の保留事項とする。

---

# 19. 補助特徴量

各5秒評価区間について、以下を計算・保存する。

## 19.1 帯域別特徴量

```text
A_5s(B)      各候補帯域の帯域RMS加速度
L_5s(B)      各候補帯域のdB値
Q_dB         帯域比
peak_freq    PSDが最大となる周波数
band_energy  帯域パワー（積分値）
```

## 19.2 サブ窓ベースの分散指標

サブ窓ごとの帯域RMS `A_sub(l)` から算出する。

```text
A_sub_max     = max( A_sub(1..L) )
A_sub_median  = median( A_sub(1..L) )
A_sub_std     = sqrt( (1/L) · Σ_l ( A_sub(l) - mean(A_sub) )² )
```

平方根を含むことに注意する（第1版では平方根が欠落していた）。

これらは主判定に使用せず、誤検知分析・見逃し分析（第46章・第47章）のために保存する。

## 19.3 時間領域特徴量

```text
RMS          5秒区間の全帯域RMS
Peak         5秒区間の絶対値最大
crest_factor Peak / RMS
```

## 19.4 品質指標

```text
clip_rate               当該区間の飽和サンプル率
n_valid_subwindows      有効サブ窓数
```

## 19.5 3軸比較

必要に応じて ax, ay, az の3軸について同様の特徴量を計算し、主解析軸の妥当性を検証する。

ただし最終的なMVP判定式は可能な限り単純に保つ。

---

# 20. 走行中ゲート

## 20.1 目的

停車中・押し歩き中・信号待ちの区間は振動が小さく、`L_5s` が低下する。これらが評価対象へ混入すると、重度パンクの見逃しの直接原因となる。

## 20.2 判定条件

```text
評価対象とする条件：  A_5s(B_all) >= T_move

B_all  ：全解析帯域（2 Hz <= f <= fN）
T_move ：停止時データの99パーセンタイルから決定
```

`B_all` の上限は `fs` に依存する。`fs` は第58章の保留事項であるため、確定後に bin 番号で定義する。

## 20.3 除外の記録

条件を満たさない区間は評価対象外とし、以下を記録する。

```text
excluded = true
excluded_reason = "not_moving"
```

除外区間は `n_segments` に含めない。

## 20.4 パラメータの決定

`T_move` は実験前に、施錠状態および押し歩き状態のデータから決定する。センサーのみで実現可能であり、追加ハードウェアを必要としない。

## 20.5 走行中ゲートと走行時間の関係

本節は第3版で新設した。

走行中ゲートで除外された区間は `n_segments` に含めない。したがって、信号待ち等が発生した走行では、走行時間（`duration_s`）が長くても評価区間数は増えない。

第42章の走行時間による統制は「実質走行時間」に対して定義されるものではないため、停車を含む走行は走行時間帯域を逸脱しやすい。この場合、走行中ゲートによる除外と走行時間による除外の両方が働くため、判定基準の優先順位を以下のとおりとする。

```text
1. まず走行中ゲートにより非走行区間を除外する
2. 除外後の有効区間数 n_segments が 6 未満の走行は解析対象外とする
3. 走行時間による除外判定は、停車が発生しなかった走行に対して適用する
```

停車が発生した走行は `notes` へ記録し、走行時間による除外判定からは切り離す。

---

# 21. 解析周波数帯域の決定

## 21.1 帯域は未確定である

```text
解析帯域 B* ：未確定
```

第1版では 20〜40 Hz を主要解析候補帯域としていたが、第2版以降ではこれを筆頭候補から降格し、完全に未確定として扱う。

理由：

1. 第1版の根拠データは実効サンプリング周波数が 62〜71 Hz であり、ナイキスト周波数（31〜36 Hz）が 20〜40 Hz 帯の上半分を下回っていた。当該帯域は物理的に存在せず、折り返し成分を観測していた
2. 提供された比較データは合成データであり、異常成分は 15 Hz および 20 Hz に存在した。20〜40 Hz が差を示したのは、20 Hz 成分の漏れ込みとノイズ床の一律2倍によるものであった
3. 同データにおける分離比は 12〜22 Hz が 15.1 倍、20〜40 Hz が 6.5 倍であった

## 21.2 決定手順

### 手順1：bin別AUCプロファイルの算出

候補帯域を先に決めるのではなく、まず各周波数 bin 単独での分離性能を算出する。

**評価単位は走行単位とする。** 第2版はこの点が第8章および第24.3章と整合していなかった。区間単位で AUC を算出すると、1走行から9〜12個のサンプルが得られることになり、実質的なサンプル数が10倍に見積もられ、帯域選択の段階で楽観バイアスが入る。

```text
各走行 r、各 bin k について：
  L_r(k) = median_{i ∈ segments(r)} [ 20·log10( sqrt( G_i(k)·Δf ) ) ]
  ↓
  正常群 { L_r(k) | r ∈ 正常 } と
  重度群 { L_r(k) | r ∈ 重度 } から AUC(k) を算出
  ↓
  周波数に対する AUC のプロファイルを図示
```

走行内代表値には中央値を用いる。最大値は単発の外乱に、平均値は分布の裾に影響されやすいためである。ただし、中央値・平均値・第75パーセンタイルの3種を算出し、プロファイルの形状が代表値の選択に依存しないことを確認する。

区間単位の bin別AUC は、走行内のばらつきを把握するための補助的な図示としてのみ算出してよい。帯域選択の根拠には使用しない。

### 手順2：候補帯域の切り出し

AUCプロファイルにおいて AUC が高い連続領域を、bin 境界で切り出して候補帯域とする。

候補帯域は互いに重複しないよう定義することを原則とする。重複する帯域どうしは強く相関し、AUCの差が実験ノイズに埋もれるためである。

### 手順3：帯域の決定

```text
B* = argmax_B  AUC(B)
```

AUC(B) の算出も走行単位で行う。

ただし、AUCの95%信頼区間（ブートストラップ法、run_id単位でリサンプリング）が重なる帯域が複数存在する場合は、以下の優先順位で選択する。

```text
1. FPR_run <= 25% を満たす条件で Recall_run が高い帯域
2. より低域側の帯域（エイリアシングの影響が小さい）
3. より狭い帯域（物理的解釈が明確）
```

## 21.3 帯域変更時の再評価

解析帯域を変更した場合、閾値および全性能指標を再計算する。過去の帯域における特徴量および閾値を転用してはならない。

## 21.4 帯域選択はネスト交差検証の内側で行う

本節は第3版で新設した。

21.2節の手順は、第45章のネスト交差検証における**内側ループの内部で実行される**。外側テストfoldのデータを含めて bin別AUCプロファイルを算出してはならない。

全データを用いた bin別AUCプロファイルは、報告および解釈のために別途図示してよい。ただしその図から読み取った帯域を性能評価に用いてはならない。

---

# 22. 区間判定

5秒評価区間 i における判定を以下で定義する。

```text
Y_seg(i) = 1   if  L_5s(i) >= T*
Y_seg(i) = 0   if  L_5s(i) <  T*
```

ここで `T*` は第24章のROCにより決定する閾値（dB）である。

区間判定は走行単位判定の入力であり、**区間判定単体を最終判定として使用しない**。

---

# 23. 走行単位判定への集約

本章は第2版で新設した、本仕様書における最重要項目である。

## 23.1 集約規則

```text
Y_run(r) = 1   if  Y_seg が k 区間以上連続して発生
Y_run(r) = 0   otherwise
```

ここで、

```text
r ：走行ID（run_id）
k ：連続判定回数（暫定値：k = 2）
```

走行中ゲート（第20章）で除外された区間は連続判定においてカウントせず、かつ連続性を断ち切らないものとする。

## 23.2 集約規則が必要な理由

第1版には、区間単位の判定を走行単位の判定へ変換する規則が存在しなかった。一方でラッチ機構（第28章）は「異常判定が成立した時点で確定」であるため、実装上は暗黙に `k = 1`（OR条件）となっていた。

`k = 1` の場合、1走行あたりの区間数を n とすると、走行単位の誤検知率は以下となる。

```text
FPR_run = 1 - ( 1 - FPR_seg )^n
```

| FPR_seg | n = 10 の FPR_run（k = 1） |
|---:|---:|
| 25% | 94.4% |
| 10% | 65.1% |
| 5% | 40.1% |
| 2.8% | 25.0% |

すなわち `k = 1` では、区間単位の誤検知率を 2.8% 以下に抑えない限り、走行単位で 25% を達成できない。

## 23.3 k = 2 における定量（独立仮定）

本節は第3版で新設した。

誤検知が区間間で独立に発生すると仮定した場合、`k = 2` における走行単位誤検知率は以下となる。

| FPR_seg | n = 10 の FPR_run（k = 2） |
|---:|---:|
| 25% | 38.8% |
| 19% | 25.0% |
| 10% | 8.0% |
| 5% | 2.2% |

`k = 1` では区間単位で 2.8% 以下が必要であったのに対し、`k = 2` では 19% 程度まで許容される。要求水準がおよそ 7 倍緩和される。

一方、**重度パンクは走行中一貫して継続するため、Recall はほとんど低下しない**。この非対称性を利用するのが本章の主旨である。

## 23.4 独立仮定に関する注意

本節は第3版で新設した。

23.3節の計算は「誤検知が区間間で独立に発生する」ことを前提としている。しかし実際の誤検知の主因は路面（段差の連続、荒れた舗装のストレッチ、インターロッキングの連続区間）であると想定され、これらは**時間的に連続して発生する**。

すなわち、正常走行における `Y_seg = 1` は独立ではなく正の相関を持つ可能性が高く、その場合 `k >= 2` による誤検知抑制効果は 23.3 節の表より小さくなる。

したがって、

```text
1. 23.3節の表は k の探索範囲を決める目安としてのみ用いる
2. 実際の FPR_run は必ず実測データから算出する
3. 正常走行における Y_seg の連続長の分布（max_consecutive のヒストグラム）を
   路面種別ごとに図示し、独立仮定からの乖離を定量する
```

第29.2章の2走行ルールについても同様である。同一ルート・同一ライダーで走行する実験条件下では、走行間の誤検知も独立ではない。

## 23.5 k の決定

`k` は固定値ではなく、閾値 `T*` と同時に最適化する（第24章）。

```text
k の探索範囲：1, 2, 3, 4
```

`k` を大きくすると、評価区間数 `n` が少ない走行（停車が多い走行、走行時間が短い走行）では Recall が低下する。`n` の分布と `k` の関係を必ず確認する。

## 23.6 記録項目

各走行について以下を記録する。

```text
n_segments            評価対象区間数
n_positive_segments   Y_seg = 1 となった区間数
max_consecutive       Y_seg = 1 の最大連続数
Y_run                 走行単位判定結果
```

`max_consecutive` は、`k` を事後的に変更した場合の性能を再計算するために必須である。また第29.2章の2走行ルールの事後シミュレーションにも用いる。

---

# 24. ROCによる閾値決定

## 24.1 探索空間

閾値 `T*` と連続判定回数 `k` を2次元グリッドで同時探索する。

```text
T ：L_5s の実測範囲を 0.5 dB 刻みで走査
k ：1, 2, 3, 4
```

## 24.2 目的関数

```text
(T*, k) = argmax  Recall_run(T, k)
          subject to  FPR_run(T, k) <= 0.25
```

すなわち「正常走行の走行単位誤検知率 25% 以下を維持できる範囲で、重度パンクの走行単位検知率が最大となる組み合わせ」を選択する。

制約に用いる `FPR_run` は `FPR_strict`（第51.3章）とする。

## 24.3 評価単位

ROCによる閾値決定は、**走行単位の正解ラベルを基準として行う**。

同一走行から得られた複数の5秒評価区間を、独立した評価サンプルとして扱ってはならない。

## 24.4 制約を満たす解が存在しない場合

`FPR_run <= 0.25` を満たす解が存在しない場合、以下の順で対処する。

```text
1. 解析帯域 B* の再検討（第21章へ戻る）
2. 取得系・取り付け条件の再確認（第11章・第12章）
3. 第二特徴量 Q_dB の導入（第26章）
4. 目標値そのものの再設定（第7章 7.3節）
```

閾値のみを機械的に調整して見かけ上の数値を満たすことは行わない。

## 24.5 同点時の選択規則

本節は第3版で新設した。

グリッド探索において Recall_run が同値となる候補が複数存在する場合、以下の優先順位で選択する。

```text
1. FPR_run がより小さい組み合わせ
2. k がより小さい組み合わせ（実装が単純、n が少ない走行への頑健性）
3. T* がより中央に位置する組み合わせ（実測分布の端に閾値を置かない）
```

閾値が `L_5s` の実測分布の端（最小値・最大値の近傍）に決定された場合、その値は外挿であり信頼できない。この場合は候補から除外し、記録に残す。

---

# 25. 累積検知率

## 25.1 定義

重度パンクは修理されるまで継続するため、検知機会は利用回数とともに累積する。

```text
P_detect(n) = 1 - ( 1 - Recall_run )^n
```

`n` は当該車両の利用回数。

## 25.2 Recall_run = 65% における累積検知率

| 利用回数 n | 累積検知率 |
|---:|---:|
| 1 | 65.0% |
| 2 | 87.8% |
| 3 | 95.7% |
| 4 | 98.5% |

走行単位 Recall 65% は、**3回の利用で 95.7% の車両を検知できる**ことを意味する。これが本システムの実質的な性能指標である。

## 25.3 運用上の留意点

一方、誤検知も同様に累積する。走行単位 FPR 25% の場合、正常車両が n 回の利用のうち少なくとも1回誤検知される確率は以下となる。

| 利用回数 n | 累積誤検知率（2走行ルール非適用時） |
|---:|---:|
| 1 | 25.0% |
| 2 | 43.8% |
| 3 | 57.8% |

第2版の表見出しは「k = 1、ラッチ即時」であったが、`FPR_run` を所与とすればこの累積計算は `k` に依存しない。第3版では「2走行ルール非適用時」へ修正した。

第29章の SUSPECT 状態および2走行ルールは、この累積を抑制するための機構である。

## 25.4 独立仮定に関する注意

25.1節および25.3節は、走行間の判定が独立であることを前提としている。同一車両を同一利用者が繰り返し利用する場合、また特定のルートに誤検知を誘発する路面がある場合、走行間の独立性は成立しない。

実運用データが得られた段階で、この仮定の妥当性を検証する。

## 25.5 報告義務

PoC結果の報告においては、走行単位の Recall_run / FPR_run に加えて、累積検知率および累積誤検知率を必ず併記する。

---

# 26. 追加特徴量の導入条件

## 26.1 導入しない条件

`L_5s` 単体で、

```text
Recall_run >= 65%   かつ   FPR_run <= 25%
```

を満たす場合、追加特徴量を主判定へ導入しない。

## 26.2 導入順序

目標を満たさない場合、以下の順に評価する。

```text
1. Q_dB          帯域比
2. A_sub_std     サブ窓間のばらつき
3. A_sub_max     サブ窓最大値
4. crest_factor  波高率
5. 他の周波数帯域の追加（2帯域併用）
6. 3軸特徴量
7. 必要な場合のみスコアリング・機械学習
```

第2版の項目7「速度正規化」は削除した。本フェーズでは速度取得手段が存在しないため、実行できない。速度取得手段が確保された場合に限り、将来課題（第55章 Priority 12）として再検討する。

## 26.3 原則

追加特徴量を導入する場合も、複雑なモデルを最初から採用せず、単純なルールベース方式から検討する。いきなり複雑なAIモデルへ移行しない。

特徴量を1つ追加するごとに、探索の自由度が増え過学習リスクが上昇する。第45章の統計的評価手順を必ず適用する。

---

# 27. デバイス実装形態

## 27.1 実験フェーズとデバイス実装の分離

```text
実験フェーズ（PC上）
  FFT / Welch法 → bin別AUCプロファイル → 解析帯域 B* の決定

デバイス実装（ESP32）
  バンドパスフィルタ + RMS
```

## 27.2 デバイス側の実装

解析帯域 `B*` が確定した後、ESP32 上で FFT を実行する必要はない。

```text
y[n]  = BPF( az[n], B* )                    4次IIRバンドパスフィルタ
A_5s  = sqrt( (1/M) · Σ_{n=1}^{M} y[n]² )   M = 5 · fs
L_5s  = 20 · log10( A_5s )
```

パーセバルの定理により、理想的なバンドパスフィルタであれば、これは帯域RMS加速度と数学的に等価である。

## 27.3 利点

```text
FFTライブラリが不要      （サンプル数が2のべき乗である必要がない）
バッファが不要            （逐次計算、メモリ数十バイト）
窓関数・正規化が不要      （実装上の誤りの余地が減る）
演算量が1〜2桁減少        （消費電力の削減）
```

サンプル数が2のべき乗である必要がないという性質は、`fs` が第58章の保留事項であることに対する頑健性の点でも有利である。

## 27.4 検証手順

本節は第3版で全面改訂した。第2版は「FFT/Welch 方式との一致（誤差 0.5 dB 以内）」を要求していたが、これを撤回する。

撤回の理由：実現可能な4次IIRフィルタは阻止域の減衰が有限であり、帯域外のパワーが解析帯域より大きい条件下ではスカート漏れによる寄与だけで 0.5 dB を超える。第1版の解析では低周波帯（2〜10 Hz）が識別帯域より大きなパワーを持つことが示唆されており、この条件に該当する可能性が高い。すなわち「一致誤差 0.5 dB 以内」は達成不能な基準である可能性がある。

第3版では、BPF+RMS 方式を Welch 方式の近似ではなく、**独立した特徴量として扱う**。

```text
手順：
  1. B* に対する4次IIRバンドパスフィルタを設計する
  2. 実験データへ適用し、L_5s^BPF を算出する
  3. L_5s^BPF に対して第24章のROCを再実施し、T*_BPF と k_BPF を決定する
     （第45章のネスト交差検証を同様に適用する）
  4. Welch 方式の性能と比較する

合格基準：
  BPF+RMS 方式の Recall_run / FPR_run が Welch 方式と同等であること
  （両者の95%信頼区間が重なること）

参考報告：
  L_5s^Welch と L_5s^BPF の相関係数
  両者の差分の分布（平均・標準偏差）
```

フィルタの設計条件（次数・形式・カットオフ周波数・係数・固定小数点実装の場合はそのスケーリング）は必ず仕様として固定・記録する。

デバイスへ実装する閾値は `T*_BPF` であり、`T*` を転用してはならない。

## 27.5 第二特徴量を使用する場合

`Q_dB` を導入する場合は、`B*` と `B_ref` の2系統のバンドパスフィルタを並列実行する。演算量は2倍になるが、依然として FFT より軽量である。

---

# 28. エラーラッチ

重度異常判定が成立した場合、

```text
fault_latched = true
```

とする。

その後正常振動になっても自動解除しない。

解除権限：

```text
メンテナンス完了操作
```

のみとする。

ヒステリシスはMVPでは導入しない。

---

# 29. デバイス状態

```text
NORMAL
SUSPECT
FAULT_LATCHED
SYNC_PENDING
MAINTENANCE
CLEARED
MOUNT_FAULT
```

## 29.1 各状態の定義

### NORMAL

異常なし。当該走行において `Y_run = 0` かつ `max_consecutive = 0`。

### SUSPECT

`Y_seg = 1` となる区間が存在したが、連続数が `k` に達しなかった走行。

```text
0 < max_consecutive < k
```

ラッチせず、クラウドへ送信のみ行う。

### FAULT_LATCHED

重度異常確定。`Y_run = 1`。

### SYNC_PENDING

異常確定済みだがクラウド未送信。

### MAINTENANCE

貸出停止・整備待ち。

### CLEARED

整備担当者が確認・解除。

### MOUNT_FAULT

取り付け異常を検出（第30章）。パンク判定より優先して報告する。

## 29.2 k = 1 が選択された場合の扱い

本節は第3版で新設した。

`k = 1` の場合、`0 < max_consecutive < 1` を満たす整数は存在しないため、**SUSPECT は定義上空集合となる**。すなわち `Y_seg = 1` が1区間でも発生すれば直ちに FAULT_LATCHED となり、29.3節の2走行ルールは機能しない。

第24章のROCにより `k = 1` が選択された場合、以下のいずれかを選ぶ。

```text
案A：2走行ルールを不採用とする（k = 1 の単純ラッチ）
案B：SUSPECT を別条件で定義する
     例：n_positive_segments = 1 かつ max_consecutive = 1 を SUSPECT とし、
         n_positive_segments >= 2 を FAULT_LATCHED とする
```

案Bを採用する場合、これは実質的に「非連続を許容する k = 2」であり、性能を実測データで再評価しなければならない。いずれの案を採るかは PoC 結果により決定し、記録に残す。

## 29.3 2走行ルール（暫定）

誤検知の累積を抑制するため、以下を暫定仕様とする。

```text
SUSPECT が2走行連続で発生した場合  → FAULT_LATCHED へ昇格
```

誤検知は走行間で独立に発生しやすい一方、真のパンクは連続して発生する。この非対称性により、Recall の低下を最小限に抑えつつ誤検知を削減できる。

ただし第23.4章のとおり、実験条件下（同一ルート・同一ライダー）では走行間の独立性も成立しない可能性がある。本ルールの採否と条件は、PoC の実測データにより決定する。

## 29.4 2走行ルールの評価方法

本節は第3版で新設した。

2走行ルールは走行間の状態遷移を伴うため、単一走行を1サンプルとする第51章の合格判定とは評価単位が異なる。両者の関係を以下のとおり整理する。

```text
PoC の合否判定    ：単一走行の Recall_run / FPR_run（第51.1章）
2走行ルールの評価 ：記録済みの max_consecutive を用いた事後シミュレーション
```

事後シミュレーションの手順：

```text
1. 各走行の max_consecutive から、SUSPECT / FAULT_LATCHED / NORMAL を判定する
2. 同一クラスの走行をランダムな順序で並べ、仮想的な連続利用列を生成する
3. 2走行ルールを適用したときの検知までの走行回数、および
   正常列における誤検知発生までの走行回数を算出する
4. 1000回のリサンプリングにより分布を求める
```

この結果は PoC 報告へ必ず併記する（第51.4章）。

---

# 30. 取り付け異常・センサー自己診断

## 30.1 取り付けずれの検出

```text
基準ベクトル u_ref からの角度ずれ > 15°
        ↓
    MOUNT_SHIFT として整備対象へ
```

`u_ref` は初回設置時に記録し、車両IDに紐付けてクラウドへ保存する。

判定は静止時（走行開始前）の重力ベクトルにより行う（第12.2章）。

## 30.2 緩みの検出

第3版で推定手順を具体化した。

```text
1走行中における u の変動が閾値を超過
        ↓
    MOUNT_LOOSE として整備対象へ
```

### 30.2.1 走行中の u の推定手順

走行中の3軸平均は、重力に加えて車体の平均加速度（加減速・旋回・登坂）を含む。したがって静止時と同じ方法をそのまま適用することはできない。以下の手順による。

```text
推定窓長  ：5秒（評価区間と同一）
推定方法  ：区間内の3軸平均ベクトルを正規化して u_i を得る
有効条件  ：|3軸平均ベクトルの大きさ| が 9.8 m/s² の ±5% 以内であること
```

有効条件を満たさない区間は、加減速・旋回が大きいと判断して推定から除外する。

### 30.2.2 緩み判定

```text
theta_i     = arccos( u_i · u_ref )      各有効区間の基準からのずれ
theta_range = max(theta_i) - min(theta_i)

緩み判定：theta_range > 10°  （暫定値）
```

有効区間が3未満の走行では緩み判定を行わない。

`theta_range` は走行メタデータへ必ず記録する。閾値 10° は暫定値であり、実測データにおける正常時の分布から確定する（第58章）。

### 30.2.3 緩みを優先報告する理由

ブラケットの緩みは取り付け部の共振・打音を発生させ、広帯域のスペクトル成分を増加させる。これは誤検知の直接原因となるため、**パンク判定より優先して報告する**。

取り付け角度のずれは Recall を低下させ、緩みは FPR を上昇させる。作用方向が逆であるため、閾値調整では両方を同時に救済できない。検出して修理する以外に対処法はない。

## 30.3 その他のセンサー自己診断

定期点検時および起動時に以下を監視する。

```text
MPU6050 通信応答
異常な固定値（3軸すべてが一定）
ノイズ量（静止時の標準偏差が異常に大きい／小さい）
オフセット（静止時の |g| が 9.8 m/s² から乖離）
FIFOオーバーフローの発生
実効サンプリング周波数の逸脱
飽和率の増大
ESP32 ストレージ状態
バッテリー状態
```

センサー故障を自転車故障と誤認しない設計とする。

---

# 31. クラウド送信

## 31.1 送信タイミング

```text
FAULT_LATCHED / SUSPECT / MOUNT_FAULT の確定
        ↓
返却・施錠
        ↓
Wi-Fi接続
        ↓
Firebase送信
```

## 31.2 送信データ

```text
vehicle_id
sensor_id
run_id
timestamp
device_status
fault_type
severity

L_5s_max              走行中の L_5s 最大値
L_5s_mean             走行中の L_5s 平均値
Q_dB_max              帯域比の最大値
n_segments            評価区間数
n_positive_segments   陽性区間数
max_consecutive       最大連続陽性区間数

band_low_bin          使用した解析帯域（bin番号）
band_high_bin
band_low_hz           使用した解析帯域（周波数）
band_high_hz
threshold_db          使用した閾値 T*
k_consecutive         使用した連続判定回数 k

clip_rate             飽和率
fifo_overflow_count   FIFOオーバーフロー回数
fs_effective          実効サンプリング周波数

gravity_x             取り付け姿勢
gravity_y
gravity_z
tilt_angle_deg
theta_range           1走行中の姿勢変動幅

duration_s            走行時間
distance_m            走行距離
```

第2版の `mean_speed` は削除した。速度を測定する手段が存在しないためである（第42章）。走行距離が固定される実験条件下では `distance_m / duration_s` により平均速度が導出できるが、実運用では走行距離が固定されないため、送信項目には含めない。

---

# 32. データ通信設計

通常走行時：

```text
クラウド通信なし
```

異常時：

```text
ローカル保存
```

返却時：

```text
クラウド同期
```

とする。

目的：

- バッテリー消費削減
- 通信費削減
- ネットワーク非依存化

---

# 33. 通信障害

通信失敗：

```text
FAULT_LATCHED
        ↓
SYNC_PENDING
```

ログを端末内へ保持する。次回通信可能時に自動再送する。

---

# 34. Firebase側車両状態

```text
AVAILABLE
RESERVED
IN_USE
MAINTENANCE_REQUIRED
MAINTENANCE
```

異常送信を受信した場合、

```text
MAINTENANCE_REQUIRED
```

へ変更する。

予約APIはこの状態の車両を予約対象から除外する。

---

# 35. ユーザー申告連携

| センサー判定 | 利用者申告 | 対応 |
|---|---|---|
| NORMAL | なし | 正常運用 |
| NORMAL | あり | 点検候補 |
| SUSPECT | なし | 経過監視（2走行ルール対象） |
| SUSPECT | あり | 点検対象 |
| FAULT_LATCHED | なし | 貸出停止 |
| FAULT_LATCHED | あり | 貸出停止（優先度高） |
| MOUNT_FAULT | — | センサー整備対象 |

---

# 36. 利用者フォロー

返却時に異常が検知された場合、利用者へアンケートを送信する。

目的：

- 異常の体感確認
- センサー判定精度向上
- 故障原因収集
- CS問い合わせ削減
- 負のユーザー体験へのフォロー

アンケート回答と整備結果を将来の教師データとして利用できる設計とする。特に `FPR_field`（第51章）の実運用値の推定に用いる。

---

# 37. メンテナンスフロー

```text
異常検知
        ↓
返却
        ↓
クラウド同期
        ↓
MAINTENANCE_REQUIRED
        ↓
管理担当者確認
        ↓
メンテナンスリスト追加
        ↓
車両回収
        ↓
点検・修理
        ↓
修理結果登録
        ↓
異常フラグ解除
        ↓
AVAILABLE
```

---

# 38. 誤検知対応

誤検知だった場合、保守担当者が管理画面またはアプリから

```text
異常解除
```

を実行できる。

解除時には

```text
False Positive
```

として記録する。この情報を次回閾値改善に使用する。

解除時には、取り付け姿勢（`tilt_angle_deg`・`theta_range`）および `clip_rate` の確認を必須項目とする。取り付け起因の誤検知を閾値の問題と誤って帰属させないためである。

---

# 39. 真の状態ラベル

点検終了時には可能な限り以下の実状態を登録する。

```text
NORMAL
PUNCTURE
LOW_PRESSURE
TIRE_DAMAGE
WHEEL_DAMAGE
MOUNT_LOOSE
SENSOR_FAULT
OTHER
FALSE_ALARM
```

これにより実運用後も判定性能を継続評価できる。

---

# 40. 試験路面

## 平坦舗装路

目的：正常データ取得。

## 段差・ひび割れ舗装

目的：瞬間衝撃による誤検知確認。

## インターロッキング舗装

目的：周期的振動・連続振動への耐性確認。

3路面は同一のコース長（200 m）で構成し、路面種別ごとに区間の構成（段差の数・位置など）を文書化する。段差の位置が固定されている場合、誤検知が特定の評価区間へ集中する可能性があるため、第23.4章の連続長分布の分析において路面構成と対応付ける。

---

# 41. 試験回数

| 状態 | 1路面 | 合計 |
|---|---:|---:|
| 正常 | 20 | 60 |
| 軽度 | 20 | 60 |
| 中度 | 20 | 60 |
| 重度 | **20** | **60** |

合計：

```text
240走行
```

## 41.1 重度パンクの走行回数を増やす理由

第1版では重度パンクのみ 1路面あたり 10走行（合計30走行）としていたが、これを 20走行（合計60走行）へ変更した。

理由：閾値決定用データと最終評価用データを分離すると評価用は約30走行となる。30走行で Recall 65% を観測した場合の95%信頼区間はおよそ 45〜81% であり、合格判定に十分な精度を持たない。

重度パンク状態は空気を抜くだけで再現でき、最も低コストで増やせるデータである。最重要クラスに最少データを割り当てる構成は避ける。

## 41.2 データの用途分離

```text
帯域選択用   ：内側交差検証
閾値決定用   ：内側交差検証
最終評価用   ：外側交差検証
```

詳細は第45章に定める。

## 41.3 走行順序のランダム化

本節は第3版で新設した。

```text
240走行の実施順は、(状態 × 路面) の組み合わせ単位でランダム化する。
状態ごとにまとめて実施してはならない。
```

### 41.3.1 理由

気温・路面湿潤・タイヤ劣化・ライダー疲労・センサー取り付け状態は時間とともに変化する。状態ごとにブロック実施すると、これらの要因が状態と完全に交絡し、観測された分離性能が振動特性ではなく実施時刻の差を反映している可能性を排除できなくなる。

特に重度パンク（0〜50 kPa）で 200 m を 60 走行すると、タイヤおよびリムが実際に劣化する。60走行を連続して実施した場合、後半の走行は前半とは異なる物理状態を測定していることになる。

### 41.3.2 実施上の制約と現実的な運用

空気圧の変更には時間を要するため、完全なランダム化は現実的でない。以下を最低条件とする。

```text
1. 同一の空気圧状態を連続して実施する場合、路面の順序をランダム化する
2. 1セッション内で少なくとも2種類以上の状態を実施する
3. 状態の実施順をセッションごとに入れ替える
4. 全240走行を1〜2セッションに集中させない
```

### 41.3.3 記録項目

```text
run_order         全240走行を通した実施順の連番
session_id        実施日・実施セッションのID
session_order     セッション内の実施順
pressure_kpa      各走行の直前に実測した空気圧
```

`run_order` と `L_5s` の相関を必ず確認する。有意な相関が見られた場合、時間依存の交絡要因が存在することを意味するため、原因を特定するまで性能評価を確定しない。

## 41.4 タイヤ状態の記録

重度パンク走行の反復によるタイヤ・リムの劣化を追跡するため、以下を記録する。

```text
tire_id           使用タイヤの識別子
tire_run_count    当該タイヤの累積走行回数
tire_condition    目視点検結果（正常 / 摩耗 / 損傷）
```

タイヤを交換した場合は `tire_id` を更新し、交換時点を記録する。

---

# 42. 走行条件

本章は第3版で全面改訂した。

## 42.1 改訂の理由

第2版は速度を 12〜15 km/h に統制し、サイクルコンピュータまたはスマートフォンGPSにより確認することを求めていた。

しかし現時点で速度を計測する手段が確保されていない。計測手段を前提とした統制条件は実行できないため、**速度という量を直接扱わない方式へ変更する**。

## 42.2 走行距離

```text
200 m / 1試行（固定）
```

走行距離は全走行で固定する。コースの始点・終点を物理的に明示し、全走行で同一の区間を走行する。

## 42.3 走行時間による統制

速度の代わりに走行時間を統制量として用いる。

```text
統制量：duration_s（走行時間）
取得方法：センサーログのタイムスタンプ（走行開始 / 終了マーカー間）
```

走行距離が固定されるため、走行時間は平均速度と一対一に対応する。追加のハードウェアを必要としない。

### 42.3.1 走行時間の受入帯域（暫定）

```text
48 s <= duration_s <= 60 s
```

この帯域は、200 m を一般的なシェアサイクルの巡航速度で走行した場合に相当する。

| 走行時間 | 相当する平均速度 |
|---:|---:|
| 40 s | 18.0 km/h |
| 48 s | 15.0 km/h |
| 55 s | 13.1 km/h |
| 60 s | 12.0 km/h |
| 72 s | 10.0 km/h |

受入帯域を逸脱した走行は、記録したうえで解析対象から除外する（`excluded_reason = "duration_out_of_range"`）。

### 42.3.2 参照値としての一般的走行速度

本仕様における速度の扱いは、計測値ではなく設計上の参照値である。

```text
一般的なシェアサイクルの巡航速度：12〜18 km/h
本実験の想定帯域                ：12〜15 km/h
```

この参照値は、走行時間の受入帯域を設定するための根拠としてのみ用いる。個々の走行の速度を主張する根拠としては用いない。

### 42.3.3 走行中の統制手順

走行中は速度を確認できないため、以下の手順による。

```text
1. ライダーは「一定のペースで漕ぎ続ける」ことのみを守る
2. 加減速・停車・惰行を意図的に行わない
3. 走行後に duration_s を確認する
4. 受入帯域を逸脱した場合は、その走行を除外し、再走行する
```

事前に数回の練習走行を行い、受入帯域に入るペースをライダーが体得しておく。

## 42.4 速度変動が残ることの影響

帯域RMS加速度は速度に対して概ね1〜2乗で依存する。対数表現では以下となる。

```text
2乗依存の場合：40 · log10( v2 / v1 )

12 km/h → 15 km/h ：40 · log10(15/12) = 3.9 dB
12 km/h → 18 km/h ：40 · log10(18/12) = 7.0 dB
```

受入帯域（12〜15 km/h 相当）内であっても最大 3.9 dB の変動が残る。これは狙っている正常・異常間の分離幅と同オーダーになりうる。

さらに、**走行時間による統制は走行平均に対してのみ有効であり、5秒区間ごとの速度変動は統制できない**。区間内の加減速は補正不能な変動要因として残る。

したがって以下を仕様とする。

```text
1. 速度変動は L_5s の分散を増大させる既知の要因として明記する
2. 分布の重なりが大きい場合、原因候補として必ず速度変動を挙げる
3. ただし本フェーズでは速度を測定しないため、切り分けは不可能である
4. duration_s と L_5s の相関を必ず算出し、報告に含める
```

`duration_s` と `L_5s` の間に強い相関が観測された場合、それは速度が主要な変動要因であることの間接的な証拠となる。この場合、速度計測手段の導入を Priority 12 から繰り上げることを検討する。

## 42.5 速度正規化について

速度正規化は本仕様の対象外とする。速度取得手段（車輪回転センサー等）が確保された場合に限り、将来課題として再検討する（第55章 Priority 12）。

## 42.6 その他の統制条件

```text
車両     ：同一車両を基本とし、車両IDを記録
ライダー ：体重を記録
センサー ：同一個体・同一取り付け位置・同一取り付け角度
タイヤ   ：同一銘柄
天候     ：記録（路面が湿潤の場合は別途記録）
気温     ：記録
```

## 42.7 単一条件による外挿限界

本節は第3版で新設した。

42.6節の統制条件下では、本実験は単一車両・単一ライダー・単一センサー個体で実施される。

したがって得られる `Recall_run` / `FPR_run` は、

```text
「当該車両・当該ライダー・当該センサー個体における値」
```

であり、**フリート全体の性能推定値ではない**。

第55章 Priority 9 は「車両・ライダー体重による性能差の分析」を掲げているが、単一条件のデータからこの分析は実行できない。以下のいずれかを選択する。

```text
案A：本実験の段階で 2車両・2ライダーを含める
     240走行の内訳を調整し、各条件のサンプル数を確保する
     推奨。個体差の大きさを早期に把握できる

案B：単一条件のまま実施する
     PoC 報告に外挿限界を明記する
     Priority 9 は追加データ取得を前提とした別フェーズとする
```

いずれの案を採るかを実験開始前に決定し、記録に残す。案Bを選択した場合、PoC の結論は「本条件下で成立した」という限定付きのものとなることを、報告書の冒頭に明記する。

---

# 43. 実験ログ

各走行に一意な `run_id` を付与する。

```text
命名規則： {状態}_{路面}_{連番}

N ：正常（Normal）
L ：軽度（Light）
M ：中度（Medium）
S ：重度（Severe）

FLAT  ：平坦舗装路
STEP  ：段差・ひび割れ舗装
BLOCK ：インターロッキング舗装
```

例：

```text
N_FLAT_001
N_STEP_001
L_FLAT_001
M_BLOCK_001
S_FLAT_001
```

`run_id` の連番は状態・路面ごとの通し番号であり、実施順を表さない。実施順は `run_order` により別途記録する（第41.3.3章）。

---

# 44. データ保存形式

## 44.1 生データCSV

```text
sample_index    サンプル通し番号（0始まり）
t_us            再構成したサンプル時刻（µs、第11.3章）
ax
ay
az
gx              （ジャイロ。将来の姿勢推定・転倒検知用）
gy
gz
```

第2版の `timestamp_ms` から変更した。理由は第11.4章に記載する。

FIFO 読み出し単位のログ（読み出し時刻・取得サンプル数・FIFO残量）は別ファイルへ保存する。

```text
read_id
read_timestamp_us
n_samples_read
fifo_count_before_read
```

ヘッダに以下をコメント行として記録する。

```text
# unit: m/s2, rad/s
# time_unit: us
# fs_nominal_hz: 128
# fs_effective_hz: <実測値>
# afs_sel: 1 (+-4g)
# dlpf_cfg: 3 (44Hz)
# clksel: 1 (PLL Gyro X)
# acquisition: fifo_burst
# run_id: S_FLAT_001
```

## 44.2 走行メタデータ

別CSVまたは実験管理表へ保存する。

```text
run_id
run_order
session_id
session_order
vehicle_id
rider_id
sensor_id
tire_id
tire_run_count
tire_condition
tire_state
pressure_kpa
surface_type
sensor_position
mount_reference_id
gravity_x
gravity_y
gravity_z
tilt_angle_deg
theta_range
fs_nominal_hz
fs_effective_hz
afs_sel
dlpf_cfg
clksel
fifo_overflow_count
n_dropped
read_interval_max_ms
clip_rate
axis
distance_m
duration_s
rider_weight_kg
temperature
weather
n_segments
excluded
excluded_reason
notes
```

第2版の `mean_speed` / `max_speed` / `min_speed` は削除した（第42章）。走行距離が 200 m に固定されるため、必要に応じて `distance_m / duration_s` により平均速度を導出できるが、これは計測値ではなく導出値であることを明記する。

`dt_mean_ms` / `dt_std_ms` / `dropout_rate` は、ポーリング方式を用いた場合にのみ記録する（第11.4章）。

## 44.3 5秒評価区間データ

各区間について以下を保存する。

```text
run_id
segment_id
segment_start_us
segment_end_us
n_valid_subwindows
excluded
excluded_reason

A_5s_band          帯域RMS加速度（m/s²）
L_5s               主判定特徴量（dB）
A_5s_ref           参照帯域RMS
Q_dB               帯域比
A_sub_max
A_sub_median
A_sub_std
RMS
Peak
crest_factor
peak_freq_hz
clip_rate
u_valid            30.2.1節の有効条件を満たすか
theta_i            当該区間の姿勢角

Y_seg              区間判定結果
```

候補帯域が複数ある場合は、帯域ごとに `A_5s` と `L_5s` を保存する。

第2版にあった区間ごとの速度は、計測手段が存在しないため保存項目に含めない。

## 44.4 走行単位データ

```text
run_id
n_segments
n_positive_segments
max_consecutive
Y_run
ground_truth
threshold_db
k_consecutive
band_low_bin
band_high_bin
band_low_hz
band_high_hz
fold_id            （交差検証における分割ID）
```

## 44.5 データ分離の原則

```text
帯域・閾値決定用データと最終評価用データは run_id 単位で分離する。
同一走行から取得した複数の評価区間を、独立した評価サンプルとして扱ってはならない。
```

この原則は帯域選択（第21.2章）にも適用される。

## 44.6 合成データの扱い

アルゴリズムの単体テストのために合成データを用いる場合、以下を厳守する。

```text
1. ファイル名または run_id に SYNTH_ を接頭辞として付与する
2. 生成条件（周波数成分・振幅・ノイズ量・サンプリング条件）を必ず記録する
3. 閾値決定および性能評価には一切使用しない
```

第1版の解析において、実測データと合成データが区別なく比較され、単位も異なる状態で扱われていた。合成データは正常と異常が完全に分離するため、いかなる帯域・閾値でも AUC = 1.000 となり、性能評価の用をなさない。

---

# 45. 統計的評価手順

## 45.1 交差検証の構成

`run_id` を group とするネストされた交差検証を用いる。

```text
外側ループ（性能評価）
  └─ 内側ループ（帯域選択・閾値決定・k決定）
```

```text
外側：5分割
内側：4分割
```

分割は **(状態 × 路面) で層化する**（StratifiedGroupKFold）。第2版は GroupKFold を指定していたが、この方式は層化を行わないため、外側 fold が特定の路面や状態へ偏る可能性がある。240走行を5分割すると1 fold あたり48走行、状態ごとには12走行となり、偏りの影響は小さくない。

外側の各分割において、テストfoldのデータは帯域選択・閾値決定に一切使用しない。

## 45.2 なぜネスト構成が必要か

帯域を選び、閾値を選び、`k` を選ぶという3段階の探索を同一データ上で行うと、選択バイアスにより性能が楽観的に見積もられる。特に本プロジェクトのようにサンプル数が限られる場合、この影響は大きい。

## 45.3 信頼区間の報告

Recall_run および FPR_run は、必ず95%信頼区間を付して報告する。

```text
二項比率の信頼区間：Wilson score interval
AUCの信頼区間      ：ブートストラップ法（run_id単位でリサンプリング、1000回）
```

## 45.4 報告フォーマット

```text
Recall_run  = 68.3%  [95%CI: 55.0 - 79.4]   n = 60
FPR_strict  = 21.7%  [95%CI: 12.9 - 34.0]   n = 60
FPR_field   = 28.9%  [95%CI: 21.5 - 37.6]   n = 180
AUC(B*)     = 0.87   [95%CI: 0.79 - 0.93]
```

点推定値のみの報告は認めない。

## 45.5 多重比較への配慮

候補帯域の数が多いほど、偶然に高い AUC を示す帯域が現れやすくなる。

```text
1. 候補帯域は互いに重複しないよう定義する
2. 候補帯域数は原則5以下とする
3. AUCの信頼区間が重なる帯域どうしは「差がない」と判断する
```

## 45.6 最終パラメータの確定手順

本節は第3版で新設した。

第2版は、ネスト交差検証後に実装へ載せるパラメータをどう決めるかを規定していなかった。外側 fold ごとに `B*` / `T*` / `k` が異なりうるため、この手順がないと確定できない。

```text
原則：外側交差検証は性能の推定値を与えるものであり、
      実装に載せるパラメータを与えるものではない。

最終パラメータは、内側ループと同一の手順を全データへ適用して決定する。
```

手順：

```text
1. ネスト交差検証により Recall_run / FPR_run の推定値と信頼区間を得る
2. 外側5分割それぞれで選択された B* / T* / k を一覧化する
3. 全データに対して内側ループと同一の手順を適用し、最終の B* / T* / k を決定する
4. 3で得た値を実装へ適用する
5. 1で得た性能値を「3の設定の期待性能」として報告する
```

### 45.6.1 foldごとの不一致への対処

外側 fold 間で `B*` が一致しない場合、帯域選択が不安定であることの証拠として扱う。以下を検討する。

```text
1. 候補帯域をより広く取り直す（狭い帯域の細分化をやめる）
2. 全 fold で共通に選ばれた帯域を採用する
3. データ数を増やす
```

`B*` の不一致は必ず報告に含める。「全データで決めた B* が最良である」という記述のみを行い、fold 間のばらつきを伏せてはならない。

`T*` については、fold 間の標準偏差を報告する。標準偏差が 2 dB を超える場合、閾値の推定が不安定であると判断する。

## 45.7 合否判定に用いる統計量

本節は第3版で新設した。第2版は合否を点推定で見るのか信頼区間下限で見るのかを定義していなかった。

```text
合否判定は点推定値により行う。
95%信頼区間は必ず併記し、下限が目標値を下回る場合はその旨を明記する。
```

理由：n = 60 における Wilson 95%信頼区間の幅はおよそ ±12 ポイントである。信頼区間下限で 65% を要求すると、点推定で 78% 程度が必要となり、これは第7章が定めた目標より実質的に厳しい基準となる。目標値の定義を事後に変更することになるため採らない。

ただし、点推定による合格は以下を意味するに留まる。

```text
「本条件下で目標を満たす可能性が高いことが示された」
```

以下を意味しない。

```text
「目標を満たすことが確認された」
```

PoC 報告書には、この区別を明記する。

参考として、n = 60 における Wilson 95%信頼区間の幅を以下に示す。

| 点推定 | n | 95%信頼区間（概算） |
|---:|---:|---|
| 65% | 60 | 52 - 76% |
| 70% | 60 | 57 - 80% |
| 80% | 60 | 68 - 88% |
| 20% | 60 | 12 - 31% |
| 25% | 60 | 16 - 37% |

## 45.8 交絡の確認

本節は第3版で新設した。性能評価の前に、以下の相関を必ず算出する。

```text
run_order   と L_5s
duration_s  と L_5s
temperature と L_5s
tilt_angle_deg  と L_5s
tire_run_count  と L_5s（重度パンク群のみ）
```

状態と交絡する変数が見つかった場合、その変数を統制または層化しない限り、観測された分離性能は振動特性によるものと断定できない。

---

# 46. 誤検知分析

正常走行が重度パンクと判定された場合、必ず以下を確認する。

## 46.1 取得系・取り付け

```text
clip_rate
fifo_overflow_count
fs_effective
tilt_angle_deg（基準からのずれ）
theta_range（1走行中の姿勢変動、緩みの有無）
```

## 46.2 走行条件

```text
run_id
run_order（実施順に偏りがないか）
路面（および段差の位置）
duration_s（受入帯域内であったか、帯域内のどこであったか）
車両ID
ライダー体重
天候・路面状態
気温
```

第2版の「速度（目標速度帯を逸脱していないか）」は、`duration_s` による確認へ置き換えた。

## 46.3 特徴量

```text
L_5s（区間別の推移）
max_consecutive
陽性区間の位置（コース上のどこか）
Q_dB
A_sub_max / A_sub_median / A_sub_std
crest_factor
peak_freq_hz
選択された帯域 B* のスペクトル
B* 以外のスペクトル
3軸データ
```

陽性区間の位置は第3版で追加した。段差など路面の特定箇所に誤検知が集中する場合、それは閾値の問題ではなくコース構成の問題である。

## 46.4 原則

特に、**正常走行で連続して高い振動値が発生した原因**を特定する。

単純に閾値を上げるだけで問題を解決しない。取り付け起因の誤検知を閾値の問題として処理してはならない。

---

# 47. 見逃し分析

重度パンクが正常判定された場合、必ず以下を再検証する。

## 47.1 取得系・取り付け

```text
tilt_angle_deg（角度ずれによる振幅低下の有無）
clip_rate（飽和によりサブ窓が除外されていないか）
n_valid_subwindows（有効サブ窓が不足していないか）
除外された区間数と excluded_reason の内訳
fifo_overflow_count
```

第14.4.1章のとおり、重度パンクは飽和を起こしやすく、飽和による区間除外は見逃しの直接原因となりうる。`clip_rate` が高い走行で見逃しが集中していないかを必ず確認する。

## 47.2 走行条件

```text
run_id
run_order
duration_s（受入帯域の上限側、すなわち低速側に偏っていないか）
路面
空気圧（実測値）
tire_run_count（タイヤ劣化の影響）
車両ID
センサー位置
停車区間の混入（走行中ゲートの動作）
```

第2版の「速度（低速すぎなかったか）」は、`duration_s` による確認へ置き換えた。

## 47.3 特徴量

```text
L_5s（区間別の推移）
max_consecutive（k に届かなかったのか、そもそも T* に届かなかったのか）
n_segments（区間数が少なく k に届く機会がなかったのではないか）
B* 以外のスペクトル
peak_freq_hz
3軸データ
```

`n_segments` は第3版で追加した。`k` を大きく取った場合、評価区間数が少ない走行では原理的に `k` へ到達できない。

## 47.4 原則

特に、**重度パンクで L_5s が正常側へ近づいた原因**を特定する。

`max_consecutive` と `n_segments` の値により、原因が閾値 `T*` にあるのか、連続判定回数 `k` にあるのか、評価区間数の不足にあるのかを切り分ける。

---

# 48. 判定アルゴリズム改善順

精度不足の場合、以下の順で対処する。

```text
 1. 取得系の確認
    fifo_overflow_count / n_dropped / fs_effective / clip_rate が受入基準内か

 2. 取り付け姿勢の確認
    tilt_angle_deg / theta_range（緩みの有無）

 3. 走行中ゲートの確認
    停車区間が混入していないか、n_segments が十分か

 4. 交絡の確認
    run_order / duration_s / 気温 と L_5s の相関（第45.8章）

 5. 解析帯域の再検討
    bin別AUCプロファイルの再確認（走行単位で算出しているか）

 6. 閾値 T* と連続判定回数 k の再決定

 7. 第二特徴量 Q_dB の追加

 8. サブ窓ベースの分散指標の追加

 9. 時間領域特徴量（crest_factor 等）の追加

10. 2帯域併用

11. 3軸特徴量の検討

12. 必要な場合のみスコアリング・機械学習を検討
```

上位の項目を確認せずに下位の項目へ進んではならない。特に項目1〜4は、アルゴリズムの問題ではなく実装・運用の問題であり、これを放置したままアルゴリズムを複雑化しても改善しない。

第2版の項目11「速度正規化の検討」は削除した。速度取得手段が存在しないためである。

いきなり複雑なAIモデルへ移行しない。

---

# 49. 実装上の保守対策

## 49.1 防水

IPX5相当以上を初期目標とする。実製品化時は使用環境を踏まえて要求等級を再決定する。

## 49.2 耐衝撃・センサー固定

電子回路を固定し、基板破損を防ぐ。

ただし加速度センサー部分まで過剰に防振すると検出対象の振動を減衰させるため、**センサー固定条件を実験で統一する**。

センサーは車体に対して剛結合とし、緩衝材を介さない。緩衝材は共振周波数を持ち込み、スペクトルを変形させる。

## 49.3 取り付け治具

第12章の角度規定（±15°）を担保する治具を設計する。

```text
車種ごとに治具を用意する
組み付け時に tilt_angle_deg を検査項目とする
現場での再取り付け手順を文書化する
```

治具の設計にあたっては、第12.1章の横軸漏れ込みを考慮する。減衰量のみを根拠に許容角度を緩めてはならない。

## 49.4 盗難・いたずら

```text
車体内部
専用ネジ
外部からアクセスしにくい位置
```

を検討する。

## 49.5 電源

常時クラウド通信を行わない。第27章の BPF+RMS 方式により演算負荷を低減する。

## 49.6 長期故障

定期点検時にセンサー自己診断を実施する（第30章）。

---

# 50. MVP完成条件

以下が一連で動作すること。

```text
 1. MPU6050からタイマ割り込み+FIFOで加速度を取得
    （fs / レンジ / DLPF は第58章の確定後の値。CLKSEL = 1）
 2. 取得品質が受入基準を満たすことを確認（第11.2章）
 3. 静止時に重力ベクトルを記録し、取り付け角度を検証
 4. 走行中ゲートにより評価対象区間を判定
 5. 5秒ごとに解析帯域 B* の帯域RMS加速度を算出
 6. L_5s を計算
 7. 閾値 T*_BPF と比較して区間判定 Y_seg
 8. k 区間連続を条件に走行単位判定 Y_run
 9. fault_latched を保持
10. 返却・施錠時にFirebaseへ同期
11. 車両をMAINTENANCE_REQUIREDへ変更
12. 管理画面へ表示
13. 保守担当者が確認・解除可能
```

## 50.1 MVPで必須としない項目

```text
瞬間異常判定（I_t）
異常発生割合（R）
最大継続時間（D）
異常ブロック数（N_B）
異常時平均振幅
周波数集中度（第1版定義）
ESP32上でのFFT実行
ヒステリシス
速度計測・速度正規化
機械学習モデル
```

---

# 51. PoC合格条件

## 51.1 合格基準

```text
重度パンク：Recall_run >= 65%
正常      ：FPR_strict <= 25%
```

判定は点推定値により行う（第45.7章）。

## 51.2 評価単位

```text
走行試験1回を1サンプルとする。
```

同一走行から取得した複数の評価区間を独立した評価サンプルとして扱わない。この原則は帯域選択・閾値決定・性能評価のすべてに適用される。

## 51.3 FPRの二重定義

以下の2種類を必ず算出・報告する。

```text
FPR_strict : 正常（300〜400 kPa）のみを負例とした誤検知率
FPR_field  : 正常 + 軽度 + 中度 を負例とした誤検知率
```

`FPR_field` は実運用における誤検知率の推定値である。実フリートの自転車は日常的に 150〜250 kPa まで空気圧が低下しており、`FPR_strict` のみでは実運用性能を過大評価する。

軽度・中度データは第41章により各60走行取得するため、追加コストなしで算出できる。

```text
合格判定には FPR_strict を用いる。
FPR_field は参考値として必ず併記する。
```

## 51.4 併記すべき指標

```text
Recall_run（95%信頼区間付き）
FPR_strict（95%信頼区間付き）
FPR_field （95%信頼区間付き）
AUC(B*)   （95%信頼区間付き）
累積検知率 P_detect(n), n = 1..4
累積誤検知率
2走行ルール適用時の事後シミュレーション結果（第29.4章）
選択された帯域 B*（外側fold間のばらつきを含む）
決定された閾値 T* と連続判定回数 k（外側fold間の標準偏差を含む）
正常走行における max_consecutive の分布（路面別、第23.4章）
duration_s と L_5s の相関（第42.4章）
run_order と L_5s の相関（第45.8章）
BPF+RMS 方式との性能比較（第27.4章）
```

## 51.5 データ分離

ROCによる帯域・閾値決定に使用したデータと、最終性能評価に使用するデータは分離する（第45章）。

## 51.6 報告書に明記すべき限定条件

本節は第3版で新設した。

```text
1. 本結果は単一車両・単一ライダー・単一センサー個体における値である
   （第42.7章で案Aを採用した場合は該当条件を記載）
2. 速度は計測しておらず、走行時間により間接的に統制したにとどまる
3. 点推定による合格は「目標を満たす可能性が高い」ことを示す（第45.7章）
4. 3路面はいずれも実験用に選定した固定コースであり、
   実フリートの走行環境を代表するものではない
```

これらを伏せた報告を行ってはならない。

## 51.7 目標値の見直し

実証結果と保守業務コストを踏まえて目標値は変更可能とする。

特に、FPR_run 25% は「利用のたびに正常車両の4台に1台が誤検知される」水準であり、保守部門の処理能力との整合を PoC 完了後に必ず検証する。第29章の SUSPECT 状態および2走行ルールは、この負荷を抑制するための機構である。

---

# 52. 現時点の確定事項

```text
対象：
シェアサイクル

主要検知対象：
重度パンク

センサー：
MPU6050

マイコン：
ESP32

クラウド：
Firebase

取得方式：
タイマ割り込み + FIFOバーストリード

クロック源：
CLKSEL = 1（Gyro X 軸 PLL）

サンプル時刻の扱い：
FIFO読み出し時刻ではなく、fs_effective による再構成

タイムスタンプ分解能：
µs

保存単位：
m/s²（小数第3位まで）

取り付け角度：
鉛直方向に対し ±15° 以内

主解析信号：
az

生データ保存：
ax, ay, az の3軸すべて（およびジャイロ3軸）

重力ベクトル記録：
全走行で必須（静止時・走行中）

判定単位：
5秒（重複なし）

内部処理：
Welch法（1秒サブ窓、50%オーバーラップ、9サブ窓）

有効サブ窓の基準：
9サブ窓中6サブ窓以上

スペクトル正規化：
片側PSD

帯域の定義：
bin番号（周波数は併記）

主判定特徴量：
帯域RMS加速度の対数表現 L_5s（dB、A_ref = 1 m/s²）

第二特徴量：
帯域比 Q_dB

走行単位集約：
k区間連続方式

閾値決定方法：
ROC（T* と k の2次元探索）

評価単位：
走行試験1回（帯域選択・閾値決定・性能評価のすべて）

統計的評価：
run_id を group とし (状態 × 路面) で層化したネスト交差検証
95%信頼区間の報告

合否判定：
点推定値による（信頼区間は併記）

最終パラメータの確定：
全データへ内側ループと同一手順を適用

走行距離：
200 m 固定

速度の扱い：
計測しない。走行時間 duration_s により間接統制

走行順序：
ランダム化（状態ごとのブロック実施を禁止）

初期Recall目標：
Recall_run >= 65%

初期FPR目標：
FPR_strict <= 25%

異常状態：
デバイス側でラッチ

通信：
返却時Wi-Fi同期

AIモデル：
MVPでは必須としない
```

---

# 53. 現時点の暫定事項

```text
解析周波数帯域 B*：
未確定（bin別AUCプロファイルから決定。走行単位で算出）

閾値 T*：
未確定（実験データから決定）

連続判定回数 k：
2（暫定、1〜4を探索）

走行中ゲート閾値 T_move：
未確定（停止時データから決定）

走行時間の受入帯域：
48〜60 s（暫定。12〜15 km/h 相当）

有効区間数の下限：
6区間（暫定）

緩み判定閾値 theta_range：
10°（暫定）

2走行ルール：
採否を実測データで判断。k = 1 選択時の扱いは第29.2章

デバイス実装形態：
BPF+RMS 方式（Welch方式との性能同等性を検証後に確定）

搭載位置：
サドル下 / フレーム内部（実験で決定）

車両・ライダー条件：
単一条件（案B）か複数条件（案A）かを実験開始前に決定（第42.7章）

速度計測：
本フェーズ対象外。将来課題

ヒステリシス：
MVPでは導入しない
```

---

# 54. 廃止・格下げした事項

第1版・第2版から削除または降格した項目と、その理由を記録する。

## 54.1 閾値 130（旧 T_A）

```text
扱い：付録Aへ移動。Baseline としての比較対象からも除外
```

理由：

1. 算出根拠となったデータの実効サンプリング周波数は 62〜71 Hz であり、ナイキスト周波数が解析帯域 20〜40 Hz の上半分を下回っていた
2. 同データには ±2 g の飽和が 0.5〜2.5% 含まれていた
3. 参照値「正常 77.7 / 重度パンク 181.6」は、提供されたコードおよびデータから再現できなかった
4. 第2版以降では特徴量の定義そのもの（振幅平均 → 帯域RMSのdB表現）が異なるため、比較の意味を持たない

## 54.2 解析帯域 20〜40 Hz の筆頭扱い

```text
扱い：候補帯域の1つへ降格。優先度は与えない
```

理由：第21章 21.1節に記載。

## 54.3 0.5秒窓による瞬間異常判定

```text
扱い：廃止
```

廃止した特徴量：

```text
I_t     瞬間異常判定
R       異常発生割合
D       最大継続時間
N_B     異常ブロック数
A_bar   異常時平均振幅
```

理由：閾値による二値化を段階的に積み重ねる方式は、閾値ごとに恣意的なパラメータを必要とし、情報を失う。連続値のまま5秒間の代表値を求め、ROCで一度だけ閾値を決める方式へ統一した。

## 54.4 平均値除去 vs HPF の比較

```text
扱い：削除（第1版では Priority 1）
```

理由：第14章 14.3節に記載。

## 54.5 周波数集中度 C_t（第1版定義）

```text
扱い：帯域比 Q_dB へ置き換え
```

理由：第18章 18.3節に記載。

## 54.6 3軸ノルム

```text
扱い：主解析信号の候補から除外
```

理由：第13章 13.4節に記載。

## 54.7 振幅スペクトルの帯域平均

```text
扱い：帯域RMS加速度（PSDベース）へ置き換え
```

理由：第16章 16.4節に記載。

## 54.8 車輪10回転判定

```text
扱い：速度正規化の文脈へ統合し、将来課題（Priority 12）へ移動
```

理由：現構成では回転数を直接取得できない。MVPでは固定時間窓（5秒）を使用する。将来的に回転情報を取得できる場合、固定時間と車輪回転数で判定精度を比較する。

## 54.9 速度計測による走行統制（第2版）

```text
扱い：走行時間による統制へ置き換え（第3版）
```

理由：第42.1節に記載。速度を計測する手段が確保されていない。

削除した項目：

```text
サイクルコンピュータ／GPSによる速度確認
mean_speed / max_speed / min_speed の記録
区間ごとの速度
速度正規化（第2版 第26章 項目7、第48章 項目11）
```

## 54.10 dt_std / dt_max を FIFO 方式の主指標とすること（第2版）

```text
扱い：FIFO方式向けの指標へ置き換え。ポーリング方式用として参考掲載
```

理由：第11.1節に記載。

## 54.11 BPF+RMS と Welch の一致基準 0.5 dB（第2版）

```text
扱い：撤回。BPF+RMS 特徴量上での閾値再導出へ変更
```

理由：第27.4節に記載。

## 54.12 GroupKFold（層化なし、第2版）

```text
扱い：StratifiedGroupKFold（状態 × 路面で層化）へ変更
```

理由：第45.1節に記載。

---

# 55. 未解決事項

優先順位順。

### Priority 1

取得系の是正。第58章で確定するセンサ設定（fs / レンジ / DLPF）とタイマ割り込み + FIFO による取得を実装し、第11.2章の受入基準を満たすことを実測で確認する。CLKSEL = 1 を設定する。

**本項目が完了するまで、閾値決定を含む一切の性能評価を行わない。**

### Priority 2

取り付け治具を製作し、tilt_angle_deg が ±15° 以内であることを確認する。重力ベクトル記録機能（静止時・走行中）を実装する。

### Priority 3

走行中ゲート閾値 T_move を、停止時・押し歩き時のデータから決定する。

### Priority 4

正常・重度パンクのラベル付き実測データを、統制条件下で取得する（各60走行）。実施順は第41.3章に従いランダム化する。

### Priority 5

bin別AUCプロファイルを走行単位で算出し、解析帯域 B* を決定する。

### Priority 6

L_5s に対しネスト交差検証でROCを実施し、閾値 T* と連続判定回数 k を同時決定する。第45.6章により最終パラメータを確定する。

### Priority 7

FPR_strict / FPR_field を分けて算出し、累積検知率および2走行ルールの事後シミュレーションとあわせて PoC 合格判定を行う。

### Priority 8

目標未達の場合、第26章の順序に従って追加特徴量を評価する。

### Priority 9

路面・車両・ライダー体重・取り付け角度・走行時間による性能差を分析する。第42.7章で案Bを採用した場合、車両・ライダーの分析には追加データ取得を要する。

### Priority 10

BPF+RMS 方式を設計・検証し、ESP32 へ実装する。ラッチ、SUSPECT、2走行ルールを実装する。

### Priority 11

Firebase通信および管理画面とのステータス連携を実装する。

### Priority 12

軽度・中度異常を評価する。車輪回転数の取得手段を検討し、速度計測および速度正規化の導入可否を判断する。第42.4章において duration_s と L_5s に強い相関が観測された場合、本項目を繰り上げる。

### Priority 13

事故・転倒・盗難検知へ拡張する。ジャイロを併用した姿勢推定を導入する。

### Priority 14

車両ごとの自己校正（相対閾値）方式を検討する。取り付け角度・車種・タイヤ・ライダー体重の個体差をまとめて吸収できる可能性がある。

---

# 56. 次に行うべき実験

## 56.0 実験マイナス1：新規実測データの確認（第58章）

```text
目的：保留事項の確定

対象：取得済みの正常・重度パンクCSV

確認項目：
  1. 実効サンプリング周波数と、その安定性
  2. 公称サンプリング周波数の実現可能性
  3. clip_rate（レンジ設定の妥当性）
  4. 欠損・ジッタの実態
  5. 帯域ごとの分離傾向（探索的。閾値決定には使用しない）

出力：
  第58章の各項目を確定または改訂した第4版
```

本項目が完了するまで、以下の実験0以降の設定値は暫定である。

## 56.1 実験0：取得系の検証（Priority 1）

```text
目的：第11.2章の受入基準を満たすことの確認

手順：
  1. 第58章で確定した fs / AFS_SEL / DLPF_CFG を設定
  2. CLKSEL = 1 を設定
  3. タイマ割り込み + FIFO バーストリードを実装
  4. 静止状態で60秒間取得
  5. 走行状態で60秒間取得
  6. fifo_overflow_count / n_dropped / fs_effective / clip_rate を算出
  7. read_interval_max_ms を算出し、FIFO容量の50%以内であることを確認

合格基準：
  fifo_overflow_count = 0
  n_dropped = 0
  clip_rate <= 0.01%
  fs_effective が公称値の ±2% 以内
  read_interval_max_ms が FIFO容量の50%に相当する時間以内

不合格の場合：
  取得系を修正し、合格するまで次へ進まない
```

## 56.2 実験1：走行中ゲートの決定（Priority 3）

```text
目的：T_move の決定

手順：
  1. 施錠・静止状態で60秒取得
  2. 押し歩き状態で60秒取得
  3. 通常走行で60秒取得
  4. 各状態の A_5s(B_all) 分布を算出

決定：
  T_move = 静止・押し歩き時の99パーセンタイル
```

## 56.3 実験2：本実験（Priority 4）

```text
条件：
  サンプリング周波数  ：第58章により確定
  サブ窓長            ：1秒
  オーバーラップ      ：50%
  サブ窓数            ：9
  窓関数              ：Hann窓（periodic形）
  評価時間            ：5秒（重複なし）
  走行距離            ：200 m（固定）
  走行時間            ：48〜60 s（受入帯域）
  実施順              ：(状態 × 路面) でランダム化

走行数：
  正常 60、軽度 60、中度 60、重度 60　合計240走行
```

第2版の条件「速度 12〜15 km/h」は、走行時間による受入帯域へ置き換えた。

各5秒評価区間について、第44章 44.3節の全項目を取得する。各走行について 44.2節の全項目を取得する。

### 56.3.1 実施前チェックリスト

```text
□ ランダム化された実施順表を作成した
□ 走行時間の練習走行を実施し、ライダーが受入帯域に入るペースを体得した
□ 取り付け治具を装着し、tilt_angle_deg を検査した
□ 走行前の空気圧を実測する手順を定めた
□ 第42.7章の案A/案Bを決定した
□ コースの始点・終点を物理的に明示した
□ 走行開始／終了マーカーの記録方法を定めた
```

## 56.4 実験3：帯域決定（Priority 5）

```text
手順：
  1. 各 bin k について、各走行の区間中央値 L_r(k) を算出
  2. 正常群と重度パンク群の走行単位 bin別 AUC プロファイルを算出・図示
  3. 代表値（中央値・平均・第75パーセンタイル）の選択に対する
     プロファイル形状の頑健性を確認
  4. AUC が高い連続領域を bin 境界で切り出し、候補帯域とする
     （互いに重複しないこと、5帯域以下とすること）
  5. 各候補帯域について AUC と95%信頼区間を算出
  6. B* を決定

注意：
  1〜6 はネスト交差検証の内側ループ内で実行する（第21.4章）
  全データを用いたプロファイルは報告用の図示としてのみ作成する
```

## 56.5 実験4：閾値決定と性能評価（Priority 6・7）

```text
手順：
  1. run_id を group とし (状態 × 路面) で層化した
     外側5分割・内側4分割のネスト交差検証を構成
  2. 内側で B*、T*、k を決定
  3. 外側で Recall_run、FPR_strict、FPR_field を算出
  4. 95%信頼区間を Wilson score interval により算出
  5. 累積検知率 P_detect(n) を算出
  6. 2走行ルールの事後シミュレーションを実施（第29.4章）
  7. 全データへ内側と同一手順を適用し、最終パラメータを確定（第45.6章）
  8. 交絡の確認（第45.8章）

合格判定：
  Recall_run >= 65%  かつ  FPR_strict <= 25%（点推定値）
  信頼区間および第51.6章の限定条件を併記する
```

## 56.6 実験5：デバイス実装の検証（Priority 10）

```text
目的：BPF+RMS 方式の性能確認

手順：
  1. B* に対する4次IIRバンドパスフィルタを設計
  2. 実験2のデータへ適用し、L_5s^BPF を算出
  3. L_5s^BPF に対してネスト交差検証でROCを実施し、T*_BPF と k_BPF を決定
  4. Welch 方式の性能と比較

合格基準：
  BPF+RMS 方式の Recall_run / FPR_run の95%信頼区間が
  Welch 方式のそれと重なること

参考報告：
  L_5s^Welch と L_5s^BPF の相関係数および差分の分布
```

---

# 57. 最終目標

最終的には、

```text
利用者が故障を申告しなくても
        ↓
車両自身が異常を検知
        ↓
5秒間の帯域振動強度から重度パンクを判定
        ↓
複数区間の連続性により走行単位で確定
        ↓
異常状態をデバイス内に保持
        ↓
返却時にクラウドへ通知
        ↓
故障車を自動隔離
        ↓
次の利用者への貸出を防止
```

できるシェアサイクル向け低コスト保守支援システムを実現する。

---

# 58. 保留事項（新規実測データ確認後に決定）

本章は第3版で新設した。

以下の項目は、第2版において実験前実証データを根拠に定められたものである。当該データに依存するため、第3版では**記載内容を変更せず保留**とした。新たに取得された正常・重度パンクの実測データを確認したうえで、確定または改訂する。

## 58.1 保留項目

| No | 項目 | 第2版の記載 | 確認事項 |
|---:|---|---|---|
| 1 | サンプリング周波数 `fs` | 128 Hz | MPU6050 の分周設定で実現可能な値か。実効値の安定性 |
| 2 | 加速度レンジ | ±4 g（AFS_SEL = 1） | 実測 `clip_rate` が 0.01% 以下か。±8 g の要否 |
| 3 | 内蔵DLPF | 44 Hz（DLPF_CFG = 3） | `fs` との関係でアンチエイリアシング余裕が十分か |
| 4 | サブ窓長 `N` / 分解能 `Δf` | N = 128 / Δf = 1 Hz | `fs` 確定後に導出 |
| 5 | 全解析帯域 `B_all` の上限 | 64 Hz | `fs` 確定後にナイキスト周波数から導出 |
| 6 | ポーリング方式の受入基準値 | dt_std ≤ 1.0 ms 等 | 実測で達成可能な水準か |
| 7 | 解析帯域 `B*` | 未確定 | 実験3により決定（第56.4章） |
| 8 | 参照帯域 `B_ref` | 2〜8 Hz | 第18.5章の3条件を満たすか |
| 9 | 軽度の空気圧範囲 | 200〜250 kPa | 実測により確定 |
| 10 | 中度の空気圧範囲 | 100〜150 kPa | 実測により確定 |
| 11 | 走行中ゲート閾値 `T_move` | 未確定 | 実験1により決定（第56.2章） |
| 12 | 緩み判定閾値 `theta_range` | 10° | 正常時の分布から確定 |

## 58.2 確認手順

新規データに対して以下を実施する。第56.0章に対応する。

```text
1. タイムスタンプ列から実効サンプリング周波数を算出する
   - 全体の実効値
   - 時間区間ごとの実効値（安定性の確認）
   - サンプリング間隔のヒストグラム

2. 取得方式を推定する
   - 間隔の分布形状から、ポーリングかFIFOかを判別する
   - FIFO であれば読み出し単位のまとまりが観測されるはずである

3. 飽和の有無を確認する
   - 各軸の絶対値最大とレンジ端の関係
   - レンジ端に張り付いたサンプルの連続長

4. 単位を確認する
   - 静止区間の |a| が 9.8 m/s² 近傍か（m/s² の場合）
   - レンジに対応する LSB 値と整合するか（生LSB の場合）

5. スペクトルを確認する
   - ナイキスト周波数近傍のパワー（折り返しの兆候）
   - 正常群と重度パンク群の PSD 比較（探索的）
   - 低周波帯（2〜10 Hz）のパワーと、識別帯域との比

6. 分離傾向を確認する（探索的）
   - bin別の正常/重度パワー比
   - この結果は候補帯域の見当をつけるためのものであり、
     閾値決定および性能評価には使用しない
```

## 58.3 確認結果の扱い

```text
1. 新規データが第11章の受入基準を満たす場合
   → 第58.1章の各項目を確定し、第4版として発行する
   → Priority 4（本実験）へ進む

2. 満たさない場合
   → 取得系の是正（Priority 1）を優先する
   → 当該データは分離性能の評価に使用しない
   → 探索的な傾向把握にのみ使用し、その旨を明記する
```

いずれの場合も、**新規データを閾値決定に直接使用してはならない**。統制条件（走行距離・走行時間・実施順のランダム化・空気圧の実測）が第3版の規定に従って取得されたものでない限り、第41章〜第45章の評価手順の前提を満たさないためである。

---

# 付録A. 第1版における参考値の記録

本付録は歴史的経緯の記録であり、第3版の仕様には一切使用しない。

## A.1 第1版の閾値

```text
正常側参考値      ：約 77.7
重度パンク側参考値：約 181.6
暫定閾値 T_A      ：130
```

## A.2 使用不可とする理由

```text
1. 算出根拠データの実効サンプリング周波数が 62〜71 Hz であり、
   ナイキスト周波数（31〜36 Hz）が解析帯域 20〜40 Hz の上半分を下回っていた。
   当該帯域の観測値は折り返し成分を含む。

2. 同データには ±2 g のレンジ飽和が 0.5〜2.5% 含まれていた。
   飽和は波形を矩形波化し、存在しない高調波を全帯域に発生させる。

3. 同データにはサンプリング間隔のジッタ（標準偏差 8〜32 ms）および
   欠損（最大 23.6%）が含まれていた。

4. 参照値 77.7 / 181.6 は、提供されたコードおよびデータから再現できなかった。

5. 第2版以降では特徴量の定義が異なるため（振幅平均 → 帯域RMSのdB表現）、
   数値としての比較が成立しない。
```

## A.3 第1版の解析コードにおける問題

```text
1. 比較対象のファイル選択が誤っていた
   （実測データ vs 合成データ、単位が m/s² と生LSB で混在）

2. FFT振幅が正規化されておらず、データ長 N に比例していた

3. 実効サンプリング周波数と異なる fs = 100 Hz を仮定していた

4. 全長を一度にFFTしており、仕様書の時間窓を実装していなかった

5. 窓関数が適用されていなかった
```

---

# 付録B. 記号一覧

| 記号 | 意味 | 単位 |
|---|---|---|
| `fs` | サンプリング周波数（公称） | Hz |
| `fs_effective` | 実効サンプリング周波数 | Hz |
| `fN` | ナイキスト周波数 | Hz |
| `N` | サブ窓のサンプル数 | — |
| `L` | サブ窓数 | — |
| `Δf` | 周波数分解能 | Hz |
| `w[n]` | 窓関数（Hann、periodic形） | — |
| `X_l(k)` | サブ窓 l のFFT結果 | — |
| `G(k)` | 片側パワースペクトル密度 | (m/s²)²/Hz |
| `B` | 周波数帯域（bin集合） | — |
| `B*` | 決定された解析帯域 | — |
| `B_ref` | 参照帯域 | — |
| `B_all` | 全解析帯域 | — |
| `A_5s(B)` | 帯域RMS加速度 | m/s² |
| `A_ref` | dB の基準値（= 1 m/s²） | m/s² |
| `L_5s` | 主判定特徴量（対数） | dB |
| `L_r(k)` | 走行 r の bin k における代表値 | dB |
| `Q_dB` | 帯域比 | dB |
| `T*` | 判定閾値（Welch方式） | dB |
| `T*_BPF` | 判定閾値（BPF+RMS方式） | dB |
| `k` | 連続判定回数 | — |
| `T_move` | 走行中ゲート閾値 | m/s² |
| `Y_seg(i)` | 区間判定結果 | 0/1 |
| `Y_run(r)` | 走行単位判定結果 | 0/1 |
| `n_segments` | 評価対象区間数 | — |
| `max_consecutive` | 陽性区間の最大連続数 | — |
| `u` | 重力方向単位ベクトル | — |
| `u_ref` | 基準取り付け姿勢ベクトル | — |
| `theta_range` | 1走行中の姿勢角変動幅 | ° |
| `a_v` | 重力方向射影 | m/s² |
| `duration_s` | 走行時間 | s |
| `run_order` | 全走行を通した実施順 | — |
| `Recall_run` | 走行単位検知率 | — |
| `FPR_strict` | 誤検知率（正常のみ） | — |
| `FPR_field` | 誤検知率（正常+軽度+中度） | — |
| `P_detect(n)` | n回利用後の累積検知率 | — |

---

# 付録C. 第2版から第3版への変更対応表

| 章 | 変更内容 | 分類 |
|---|---|---|
| 0 | 確定・暫定・保留の3区分へ変更 | 構造 |
| 6 | 軽度・中度の空気圧を保留事項へ | 保留 |
| 7 | 合否判定に用いる統計量への参照を追加 | 明確化 |
| 8 | 評価単位の原則が帯域選択・閾値決定にも及ぶことを明記 | 明確化 |
| 10.1〜10.3 | 保留事項として明示（記載内容は不変） | 保留 |
| 10.5 | クロック源 CLKSEL = 1 を新設 | 追加 |
| 10.6 | 保存分解能を小数第3位へ | 変更 |
| 11 | FIFO方式向けの受入基準へ全面改訂 | 変更 |
| 11.3 | サンプル時刻の再構成手順を新設 | 追加 |
| 12.1 | 横軸漏れ込み（sin θ）を追記 | 追加 |
| 13.3 | 比較項目に漏れ込み頑健性を追加 | 追加 |
| 14.4 | サブ窓有効基準を L = 9 へ整合 | 修正 |
| 14.4.1 | オーバーラップによる伝播を新設 | 追加 |
| 15.2 | N / Δf を fs からの導出として記述 | 変更 |
| 17.2 | A_ref が慣用値と異なることを明記 | 明確化 |
| 17.3 | 速度シフトが補正不能であることを明記 | 変更 |
| 18.5 | 速度相殺の成立条件を新設 | 追加 |
| 20.5 | 走行中ゲートと走行時間の関係を新設 | 追加 |
| 21.2 | bin別AUCの評価単位を走行単位へ | 変更 |
| 21.4 | 帯域選択を内側ループで行うことを明記 | 追加 |
| 23.3 | k = 2 の定量表を新設 | 追加 |
| 23.4 | 独立仮定に関する注意を新設 | 追加 |
| 24.5 | 同点時の選択規則を新設 | 追加 |
| 25.3 | 表見出しを修正 | 修正 |
| 25.4 | 独立仮定に関する注意を新設 | 追加 |
| 26.2 | 速度正規化を削除 | 削除 |
| 27.4 | 一致基準 0.5 dB を撤回、閾値再導出へ | 変更 |
| 29.2 | k = 1 時の SUSPECT 定義を新設 | 追加 |
| 29.4 | 2走行ルールの評価方法を新設 | 追加 |
| 30.2 | 走行中の u 推定手順を具体化 | 追加 |
| 31.2 | mean_speed を削除、取得系項目を更新 | 変更 |
| 40 | 路面構成の文書化を追加 | 追加 |
| 41.3 | 走行順序のランダム化を新設 | 追加 |
| 41.4 | タイヤ状態の記録を新設 | 追加 |
| 42 | 速度統制を走行時間統制へ全面改訂 | 変更 |
| 42.7 | 単一条件の外挿限界を新設 | 追加 |
| 44.1 | タイムスタンプを µs へ、FIFOログを追加 | 変更 |
| 44.2 | 速度項目を削除、実施順・タイヤ項目を追加 | 変更 |
| 45.1 | StratifiedGroupKFold へ変更 | 変更 |
| 45.6 | 最終パラメータ確定手順を新設 | 追加 |
| 45.7 | 合否判定の統計量を新設 | 追加 |
| 45.8 | 交絡の確認を新設 | 追加 |
| 46 / 47 | 速度確認を duration_s へ、確認項目を追加 | 変更 |
| 48 | 交絡確認を追加、速度正規化を削除 | 変更 |
| 51.6 | 報告書の限定条件を新設 | 追加 |
| 54.9〜54.12 | 第2版からの廃止項目を追記 | 追加 |
| 56.0 | 新規データ確認の実験を新設 | 追加 |
| 56.3.1 | 実施前チェックリストを新設 | 追加 |
| 58 | 保留事項を新設 | 追加 |
| 付録C | 本表を新設 | 追加 |
